# Decision log

Dated record of product decisions that changed how the app behaves, as opposed to routine bug fixes or polish. See `docs/08-product-bible.md` for the standing rules these decisions established.

## 2026-07-21 — Motion/colour design language, and scoped dark mode

**Decision:** Separated "the tap" from "the state" in the home screen's animation, and built a four-palette (light/dark × normal/quiet) colour token architecture.

**What changed:**

- The Going quiet / Reconnect tap animations no longer carry any colour or shadow blending — they're now a quick (280ms), purely physical scale change. The emotional colour shift was moved onto the Taking Time state itself, as a separate, slower (1200ms), decoupled fade that triggers on arriving at or leaving that screen.
- The circle's two hardcoded pixel sizes were replaced with one base size plus a single named constant, `QUIET_CIRCLE_SCALE` (`app/index.tsx`), so the resting size is a one-line tuning knob rather than a pair of sizes to keep in sync.
- Taking Time is fully static — no motion at all, on purpose, since it's meant to be genuinely restful rather than quiet-but-animating. The "something is alive" motion belongs to Reconnect instead: the circle grows back out alongside a one-shot ripple that widens and fades (an exhale), the one moment in the flow designed to feel actively in motion.
- `src/constants/theme.ts` was restructured into four palettes (`lightNormal`, `lightQuiet`, `darkNormal`, `darkQuiet`) behind a new `useAppTheme(variant)` hook (`src/hooks/useAppTheme.ts`). All four combinations were checked against WCAG AA contrast minimums before being finalized.

**Scoping decision — dark mode:** building full app-wide dark mode support was clearly a much larger undertaking than the rest of this request, and was called out explicitly rather than silently expanded or silently dropped. The decision made: build the token architecture properly now (all four palettes exist and are correct), but wire live `useColorScheme`-driven switching into the home screen only. The other ~25 files that still import the static `theme` object are unaffected and keep their current (light) appearance regardless of device colour scheme.

**Why:** the home screen is where the Taking Time/Going quiet emotional design actually lives, so it's the place dark mode matters most for this piece of work. Rolling every other screen onto the new hook is mechanical but not free — a bug in this pass shouldn't touch 25 other files' worth of surface area at once.

**Follow-up (not yet scheduled):** roll `useAppTheme` out to the remaining files that currently import `theme` directly, so Dark Mode applies consistently across the whole app rather than just the home screen. Tracked here explicitly so it doesn't quietly get treated as "already done."

## 2026-07-21 — Hold period now ends on actual return, not on opening the Return screen

**Decision:** `endOpenHoldPeriod()` moved from a mount-effect on `app/return/mode.tsx` (fired the instant the user viewed "How would you like to come back?") to the actual completion points: sending the instant message (`app/return/instant.tsx`), and marking a Thoughtful reply as sent (`app/return/reply/edit.tsx` and `app/return/reply/index.tsx`).

**Why:** with the old timing, backing out of the Return flow before actually sending anything — e.g. opening Thoughtful reply, then pressing Back — left the Home screen showing "Going quiet" instead of "Taking time," incorrectly implying the user was no longer in a Hold when nothing had actually been sent. Ending the period only on genuine completion keeps Home's state accurate to reality regardless of how the user navigates the Return flow.

**Doc updated:** `docs/03-privacy-model.md`'s Quiet History section previously documented the old (opens-the-screen) timing explicitly; corrected to describe the new, accurate rule.

## 2026-07-24 — Four-stage journey: Going Quiet, Taking Time, Reconnect, Thoughtful Replies

**Decision:** the app's core journey is now four distinct, separate stages rather than a two-state (quiet/not-quiet) model with an undifferentiated Return flow. Home has three states — Normal ("Going quiet"), Taking Time ("Reconnect" + a new repeatable "Send an update"), and a new Post-Reconnect state ("Finish Reconnecting").

**Persisted audience, not re-picked.** Going Quiet's Circle selection is now multi-select (`GroupPicker`, `HoldFlowContext.selectedGroups`) and is frozen onto the `HoldPeriod` record at send time (`audienceCircleNames`, `audienceContacts` — new optional fields, following the same "frozen at that moment" precedent `docs/03-privacy-model.md` already documents for `recipients`). Reconnect no longer has its own Circle-picker screen at all — it always targets whoever was originally told, even after the app's been fully closed and reopened, since Home always refetches the open Hold period before the user can tap anything. This simplifies `return/mode.tsx` back towards its pre-merge shape (direct-tap options, no shared Continue button) rather than complicating it further, since the reason for the earlier Circle-picker merge no longer applies.

**Two distinct short-message concepts, deliberately separately named** to avoid conflating them: `takingTimeUpdate` (Taking Time's repeatable reassurance, sent without ending Taking Time, `app/return/update.tsx`) and `quickReconnectMessage` (Reconnect's lighter alternative to Thoughtful reply, `app/return/instant.tsx`, backed by new `QUICK_RECONNECT_MESSAGES` copy replacing the old `INSTANT_MESSAGES`).

**Post-Reconnect is a deliberate, considered exception** to the app's usual principle that once a message is sent there's nothing left to do — it's the one point in the journey where something genuinely does stay open, because sending a Quick Reconnect (rather than completing Thoughtful replies) means the user has re-engaged once but not fully. It clears automatically once every in-progress Thoughtful reply is sent, or explicitly via a gentle "Already sorted?" confirmation ("I've already replied" / "I'll reply myself," each behind one extra confirming tap / "Stay in Reconnecting," which does nothing). Tone deliberately avoids "outstanding/incomplete/overdue/pending" language throughout.

**Deferred — Messaging Channels:** per-contact preferred channels (iMessage, WhatsApp, Email, Instagram DM, Messenger, Signal) and a grouped multi-channel delivery screen were explicitly scoped out of this pass, matching the dark-mode precedent above. Today's SMS/share-sheet default is unchanged. Follow-up work, not yet scheduled: `CircleContact` gains per-channel fields, a Circle/contact-settings UI for setting preferred channels, and a delivery screen that hands off to each non-SMS channel's own share/app flow rather than sending directly (e.g. "SMS — sent / WhatsApp — continue / Instagram — continue / Email — continue").

**Also deferred:** a gentle non-urgent reminder to send a Taking Time update after some interval, explicitly marked "not required now" in the request. Tracked here so it isn't mistaken for already built.

## 2026-08-10 — Reconnect's durable marker and Conversations seeding now both set on actual send, not on opening the flow

**Decision:** `beginReconnecting()` (writes the durable `RECONNECTING_KEY` marker Home's resume logic reads) and `seedFromAudience()` (populates the Conversations list with the reconnect audience) both moved from tap-effects on `app/(tabs)/index.tsx`'s `beginReconnect()` — fired the instant "Reconnect" was tapped on Home, before the transition screen or the picker itself had even been seen — to the actual completion point: the first genuine send, inside `app/return/reconnect.tsx`'s `send()`, right after `sendOrShare()` is attempted.

**Why:** with the old timing, backing out of Reconnect before ever sending anything — tapping "Reconnect," then immediately going back — left Home showing "Continue reconnecting" (from the marker) on the next open, incorrectly implying an in-progress Reconnect session when nothing had actually been sent. Moving the marker alone surfaced a second, related instance of the same bug: with Conversations still seeded at tap-time, backing out before sending still left Home falling through to **Post-Reconnect** ("Finish Reconnecting") instead of Normal — a different wrong state, not a fix. Both are the same bug class as the 2026-07-21 `endOpenHoldPeriod()` fix above, one and two steps further into the same journey: a state-changing side effect firing on entering a screen rather than on the action that screen exists to let you take.

**Mechanism:** since `endOpenHoldPeriod()` already closes the Hold period the moment "Reconnect" is tapped (unchanged, and correct — Taking Time genuinely ends there), the period id needed a way to reach `reconnect.tsx` before `RECONNECTING_KEY` exists to point to it. `HoldFlowContext`'s `setAudience()` now also carries `reconnectPeriodId`, and a new `getHoldPeriodById()` lets Reconnect read that specific period directly. `reconnect.tsx`'s `refresh()` now prefers the durable marker when it exists (force-quit-resume, or any visit after the first send) and falls back to the id-based read otherwise — so the picker still has data on a first-ever or backed-out-and-returned visit, without anything durable being written until a message actually goes out. `seedFromAudience()` is called from `send()` using the period's full original audience, not just that send's selection, so anyone not yet individually messaged still ends up in Conversations once the coverage gate is eventually satisfied — not only whoever happened to be in the very first send.

**Back-out paths (Not now, hardware back, swipe-back) needed no explicit cleanup** — since both now only ever run inside `send()`, none of the back-out paths can leave either one behind in the first place; this is prevention, not cleanup, unlike `doClearPostReconnect`'s explicit-dismissal case (2026-07-31 entry above), which still needs its own clear since it fires after real sends have already happened.

## 2026-08-10 — "Delete my data" now actually clears everything it claims to, consistently

**Decision:** audited every persisted key in the app against `deleteEverything()` (Settings drawer). Found and fixed three real gaps: the AI-memory Layer-1 toggle stayed on after a wipe (only `deleteAllMemoryNotes()` ran, clearing the notes but not the "remember helpful details" preference itself — swapped to `setMemoryEnabled(false)`, which does both); the Hold+ "simulate active" dev/test flag was never cleared at all (added `setHoldPlusActive(false)`); and the AI proxy's anonymous per-install rate-limit id survived a wipe (added a new `deleteAiInstallId()`) — confirmed as the right call directly, not a build-time guess, since there's no free-tier AI allowance left to weigh a "keep it stable to prevent quota-reset abuse" trade-off against.

**Also confirmed and corrected:** the app already called `AsyncStorage.removeItem(HAS_SEEN_WELCOME_KEY)` inside `deleteEverything()`, forcing re-onboarding after a wipe — but left the separate `HAS_SEEN_RETENTION_NOTE_KEY` flag untouched, an inconsistency with itself. Direct decision: "Delete my data" stays content-only; removed the welcome-flag clear rather than adding a matching one for the retention flag. Checked first whether skipping onboarding after a wipe leaves the user on a screen assuming data that no longer exists — it doesn't, since a wiped-but-onboarded Home resolves to the same empty Normal state every genuinely new user sees before their first Circle, which is already a fully supported state.

**Doc updated:** hold-book's `04-ux-content/04-navigation-architecture.md` (item 12) now accurately lists what's wiped, and a new open question, "'Reset app' as a distinct action from 'Delete my data,'" records that a full fresh-install reset (content + UI flags both) is a real but not-yet-needed pattern, deliberately not built now.

## 2026-08-10 — AI-assisted drafting is Hold+-only; the shared free monthly allowance was still live in code

**Confirmed directly:** AI drafting has no free allowance at all now — a real behavioural gap, not just stale docs, since `draftService.ts`'s `createDraft`/`createReplyDraft` called the AI proxy unconditionally, with no Hold+ check anywhere (unlike `AmendWithAI.tsx`, which was already correctly Hold+-gated). Fixed by mirroring `AmendWithAI.tsx`'s exact `isHoldPlusActive()` pattern in both functions — Hold+ users try the AI proxy first, falling back to the local template on any failure exactly as before; everyone else goes straight to the local template, no network call attempted at all.

**Worker's rate limiter kept, reframed, not removed.** `worker/src/rateLimit.ts` and its config (renamed `FREE_MONTHLY_DRAFT_LIMIT` → `MONTHLY_DRAFT_SAFETY_CAP` in `wrangler.toml`/`index.ts`) are not dead code even with the client-side gate live: the Worker endpoint is reachable independent of the app's own gating (the shared `APP_CLIENT_KEY` isn't a strong secret on its own — see `06-privacy-security/02-ai-boundaries.md`), and Hold+ has no server-verifiable entitlement to check against yet, since there's no real purchase/billing system. Removing the cap entirely would leave zero server-side cost protection against a leaked key or client bug. Kept at the same value (20/month/install), reframed from a user-facing "free allowance" to a blunt safety backstop.

**Not logged here:** the underlying business decision to remove the free AI tier, and what it means for Hold+'s positioning/ARR reasoning (`hold-book/07-business/06-business-strategy.md`) — flagged in hold-book's `08-decisions/04-open-questions.md` rather than written unilaterally, since that's a real strategic re-think, not a code-reconciliation clarification.

**Doc updated:** hold-book's `06-privacy-security/02-ai-boundaries.md` and `07-business/02-pricing-principles.md` corrected to drop the free-tier AI allowance framing (cost math, the Free-tier feature list, the "no free trial" reasoning); `08-decisions/04-open-questions.md`'s "Exact free-tier AI allowance" question closed out as moot. Hold+'s "Unlimited AI-assisted drafting" value-prop line and `06-business-strategy.md`'s ARR/conversion reasoning deliberately left untouched, per direct instruction — sales-pitch/strategy language, not a factual correction.

## 2026-08-10 — Fixed AdaptiveCircleChip: nothing was actually rendering as a true circle

**Root cause found and fixed.** The prior version measured a label's rendered width via a separate, invisible `position: 'absolute'` + `opacity: 0` `<Text>`, read through `onLayout`. Inside a flex row (a horizontal `ScrollView`'s `contentContainerStyle`, or a wrapping `View`), an absolutely-positioned child with no explicit width doesn't reliably shrink-wrap to its own intrinsic content under every `alignItems`/flex combination — it can pick up ambient sizing from the surrounding row instead. In practice this meant every measured width came back far larger than the actual glyph width, so every label — including single characters like "D" — failed the circle-fits check and fell through to the pill branch every time. Fixed by measuring via `onTextLayout` on the real, always-rendered `<Text>` instead: it reports the rendered line's own width/height directly from the glyph run, independent of any container box, so it isn't subject to that failure mode. It also naturally re-fires on any relayout, including a live font-scale change, so the earlier `useWindowDimensions().fontScale`-driven reset effect was no longer needed and was removed.

**Also corrected while fixing this:** the hard-minimum diameter was being treated as a fixed size rather than a floor. Height (and circle diameter) is now `Math.max(MIN_DIAMETER, measuredTextLineHeight + verticalPadding)` — at default text size this always resolves to exactly `MIN_DIAMETER` (44pt iOS / 48dp Android, both constants, independent of font scale), but at very large accessibility text sizes the container grows to fit the taller measured line rather than clipping it, while never dropping below the minimum either way. Confirmed directly from the code/constants, not by eye: `MIN_DIAMETER` is a literal fixed value used as the floor for every dimension of every shape at every font size, so both shapes stay at-or-above 44×44pt (iOS) / 48×48dp (Android) by construction.

**Also this session:** removed hold-book's older, unbuilt "Core Circle" Library-template-category concept (`04-ux-content/04-navigation-architecture.md`) — a different, older idea using the same word as the new "Core" section heading, confusing rather than disambiguated, confirmed for removal directly. Added a small, muted "Where this comes from" link beneath the Core section's copy in Your Circles, pointing to a new Research entry ("Why 'Core' groups Close and Friends together," citing Dunbar's support-clique/sympathy-group layered model) — links to the general Research screen for now, since anchored entries aren't built yet.

**Flagged, not resolved:** the requested "render and report back" visual verification for the shape fix couldn't be performed from this session — no simulator/screenshot capability here. The measurement logic itself is verified by direct code/constant inspection above, but the actual on-screen render still needs an on-device check.

## 2026-08-10 — The shape fix above didn't actually work; found and fixed the real second bug

**On-device screenshots showed the prior fix hadn't changed anything** — every chip, including "Close" and "All," still rendered as a pill. The `onTextLayout` measurement fix from earlier the same day was itself correct (verified again this pass), but a second, separate bug made the circle branch practically unreachable: `availableCircleWidth = MIN_DIAMETER(44) − 2×HORIZONTAL_PADDING(20) = 24pt`, and "Close" at 14pt/weight 600 renders at roughly 35–40pt of text width using typical San Francisco character-advance metrics — 46–67% over that 24pt threshold, regardless of how accurately it's measured. The circle was never allowed to grow past a fixed 44pt diameter, so nothing but a single narrow character could ever pass.

**Fix:** the circle's diameter now grows to fit the label — `naturalCircleDiameter = Math.max(MIN_DIAMETER, textWidth + padding, textHeight + padding)` — floored at the hard minimum as before, now also capped at a new `MAX_CIRCLE_DIAMETER` (1.5× `MIN_DIAMETER`, i.e. 66pt on iOS) so a genuinely long label doesn't grow into an oversized circular blob; those still fall back to the pill, sized to the text as before. Worked example: "Close" needs ≈55–60pt (under 66 → circle), "All" needs ≈40–44pt (floors to 44 → circle), "D" floors to 44 → circle, "Book Club" needs ≈90–100pt (over 66 → pill).

**Still flagged, not resolved:** this is derived from the code's constants and formula plus estimated font metrics, not an actual on-device screenshot — no simulator/screenshot capability exists in this session. Needs on-device confirmation, same as the prior entry.

**Also this pass:** GroupPicker's "+ New Circle" button now shows just "+" (`accessibilityLabel="New Circle"` added so screen readers still announce it properly), position unchanged since it already sat immediately after "All." Bumped its `minWidth`/`minHeight` from 38pt to 44pt — with the label text gone, padding-plus-glyph math alone would have landed around 41pt wide, under the accessible minimum.

## 2026-08-10 — Circle-chip sizing redesigned: one fixed diameter, not grow-to-fit

**Replaced the whole floor/cap/natural-diameter model with a single fixed `STANDARD_CHIP_DIAMETER`** per direct instruction, since the grow-to-fit design (even once its two bugs were fixed) produced an inconsistent row — "D" at the bare 44pt floor, "Close" at ~57–60pt, a pill at up to 66pt, none sharing a height. `STANDARD_CHIP_DIAMETER = 48pt (iOS) / 52dp (Android)` — platform-specific by design, not one shared cross-platform value, each a flat +4 above that platform's own accessibility minimum (44pt HIG / 48dp Material). Every chip is exactly this tall now, full stop: a circle at exactly this diameter if the label's measured text fits inside it (minus padding), otherwise a pill at the exact same height with only width growing to fit the text — "the same circle, stretched wider," not a separately-sized shape. Height no longer varies with font size at all (an accepted trade-off, flagged: at very large accessibility text sizes a label's line height could exceed the fixed chip height, and `numberOfLines={1}` means it clips rather than wraps or grows the box).

**Worked table, same estimated-metrics caveat as before (14pt/weight 600, San Francisco, not an on-device measurement):**

| Label | Est. text width | Shape | Width | Height |
|---|---|---|---|---|
| "D" | ~9pt | circle | 48pt | 48pt |
| "All" | ~22pt | circle | 48pt | 48pt |
| "+" | ~9–10pt | circle | 48pt | 48pt |
| "Close" | ~37pt | pill | ~57pt | 48pt |
| "Book Club" | ~75pt | pill | ~95pt | 48pt |

All five share the same 48pt height, confirming the redesign's actual goal (row consistency) independent of which ones end up circles vs. pills.

**Also this pass:** found "+" was never actually going through `AdaptiveCircleChip` — it was still a separately hand-styled `Pressable` (the one manually patched to 44×44pt last time), which is exactly why it wouldn't have picked up this sizing change automatically. Brought it into the shared component properly: added `outline` (the bordered/transparent treatment it already had), `accessibilityLabel` (its visible "+" isn't what a screen reader should announce), and `expanded` (it toggles a creation panel, not a selection — reports `accessibilityState={{expanded}}` instead of `{checked}`) props to `AdaptiveCircleChip` to support it without special-casing. Removed the now-dead `newCirclePill`/`newCirclePillPressed`/`newCirclePillText` styles. One small, honest trade-off: the old "+" button had its own `pressed` background-darken effect that `AdaptiveCircleChip` doesn't replicate — no chip in the shared component has press feedback, consistent with how the original `CirclePill` worked too, so this isn't a new gap, but "+" specifically had it before and now doesn't.

**Still flagged, not resolved:** no simulator/screenshot capability in this session — the table above is derived from code and estimated font metrics, not confirmed on-screen. Needs on-device verification.

## 2026-08-10 — Chip size reverted toward original; press feedback added; two-flag state model; reconnect.tsx migrated

**Size, resolved with git history, not a guess.** Checked `CirclePill`'s full commit history: `minHeight: 38` from creation through deletion, unchanged. `STANDARD_CHIP_DIAMETER` reverted from 48/52 (a deliberate "+4 comfortable margin" chosen two passes ago) to 44pt (iOS) / 48pt (Android) — the bare accessibility floor itself, the closest legally-compliant value to 38 since going lower isn't defensible. Also decoupled two padding uses that were wrongly sharing one constant: the circle-fit-check budget stayed tight (`theme.spacing.sm`, so short labels can still become circles) while a pill's own rendered padding was restored to the original's generosity (`theme.spacing.md`) — the perceived "smaller than before" complaint was likely as much about tightened pill padding as raw height.

**Press feedback added to `AdaptiveCircleChip` itself** — a uniform opacity dim on every chip, applied once in the shared component rather than per call site. Directly fixes a real regression flagged last round: the old hand-styled "+" button had its own `pressed` darken effect that silently disappeared when it moved onto the shared component without one.

**Two independent state props replace the single `selected` boolean:** `isSelected` and `hasSentThisSession`, both renamed/added at every call site. Priority, in order: `isSelected` wins regardless of `hasSentThisSession` (normal fill + ring); else `hasSentThisSession` shows the softened sent look (no ring); else default. Critically, `hasSentThisSession` is never written to by a selection toggle — only an actual send changes it — so the specific case flagged as most likely to get silently wrong ("reselect an already-sent chip, then deselect without sending") is handled correctly by construction: deselecting just removes the id from the selection set, and the sent flag it never touched is still sitting there true, so the chip falls straight back to the sent look.

**Full codebase audit for every Circle/pill-style location** (cross-checked two ways: grep for `theme.radius.pill`, and separately for `chipSelected`/`chipSent`, same result both times) — reported in full to the user rather than assumed complete from memory. Three genuine Circle-selection chip sites found still hand-styled outside `AdaptiveCircleChip`: `return/reconnect.tsx` (migrated this pass — see below), `return/update.tsx` (Taking Time's "Send an update," same 3-state pattern, state currently in-memory only via `HoldFlowContext`), `(tabs)/library.tsx`'s Quick-message row (2-state only, no sent visual exists there at all despite `markQuickSent` being called). Several more pill-shaped-but-not-Circle-chip UI elements found and confirmed out of scope (Home's main action circle, Library's reply-style chips, Going Quiet's post-send status pills, the bottom tab bar, "add account" button, "Coming later" badge).

**`return/reconnect.tsx` migrated** to `AdaptiveCircleChip` — its existing `selectedIds`/`coverage.contactedIds` state already correctly implemented the two-independent-flags model, durably (via `reconnectContactedIds` on the `HoldPeriod` record, already force-quit-resistant from earlier this session's Reconnect rewrite), so this was a UI-only swap, not new logic. Removed the now-dead hand-rolled chip styles.

**Flagged, not resolved — three genuine open questions, not guessed through:**
1. Whether Taking Time's `update.tsx` should get the same durable `hasSentThisSession` treatment — it wasn't named in the user's persistence-scope clarification (which named only Going Quiet/Reconnect/Conversations), so left as-is (in-memory) rather than assumed included.
2. Whether `reconnectContactedIds` on the `HoldPeriod` record (the actual existing durable mechanism, already verified) counts as an acceptable instance of "wired into existing per-flow persistence" for Reconnect, versus literally routing through `messageDraftService.ts` specifically.
3. `library.tsx`'s Quick-message chip row selects Circles, but the user's clarification named "Conversations' per-person `StoredReply` records" for that scope — `StoredReply` is the separate, chip-less Personalise flow's per-person data, with no existing per-Circle "sent" concept to hang `hasSentThisSession` off. Left unmigrated pending clarification rather than inventing a mapping.

**Still flagged, not resolved:** no on-device verification possible from this session, same limitation as every pass before it.

## 2026-08-10 — Circle-chip migration completed: update.tsx made durable, Library derived from existing state

Answers to the three questions above, and the resulting work:

**1. `return/update.tsx` confirmed in scope** (the earlier omission was an oversight, not a deliberate exclusion). Added `HoldPeriod.updateSentCircleIds` and `holdHistoryService.markUpdateSent(circleId)`, which — like `recordPostSendChoices` — operates on whichever period is currently open rather than a captured id, since Taking Time's update happens while the period is still open (unlike Reconnect, which happens after close). `update.tsx` now reads the open period fresh via `useFocusEffect` instead of `HoldFlowContext`'s `updatedCircleIds`, which was in-memory-only and never survived a force-quit. `updatedCircleIds`/`markCircleUpdated` removed entirely from `HoldFlowContext`/`HoldFlowState` — confirmed unused anywhere else first.

**2. `reconnectContactedIds` confirmed correct for Reconnect** — the instruction meant "the same kind of durable, crash-surviving persistence already used elsewhere," not literally `messageDraftService.ts` for every flow. Each flow uses whichever existing durable store is actually correct for it.

**3. Investigated Library's Quick-message flow directly rather than guessing further.** Every selected Circle gets its own independent card — own message box, own Send button, sent independently and repeatably. That's the same "select some, send, come back for more" shape Reconnect and `update.tsx` already have, not a single shared-box bulk send, so it needed the same treatment. `hasSentThisSession` here is *derived*, not a new persisted field: a Circle chip reads as sent when every one of its people already has `ConversationPerson.completed = true` (set durably by the existing `markQuickSent`); an ungrouped person's chip reads directly off their own `.completed`. No new persistence needed — the durable signal already existed, just not surfaced at the chip level.

**All Circle-chip sites now on `AdaptiveCircleChip`**, closing the full audit from two entries ago. Dead `chip`/`chipSelected`/`chipText`/`chipTextSelected`/`chipSent`/`chipSentText` styles removed from both `update.tsx` and `library.tsx`.

**Still flagged, not resolved:** no simulator/screenshot capability in this session — everything above is verified by code inspection (`tsc`/`vitest` both pass) and reasoning about existing, already-durable data, not an on-screen render. Needs on-device confirmation across all three newly-migrated screens.

## 2026-08-10 — Build-pipeline sanity check confirmed fine; "+" pinned outside the scroll

**Diagnostic requested and confirmed:** a deliberately absurd, temporary change to `AdaptiveCircleChip` (100pt diameter, magenta fill) was made, confirmed visible on-device after a force-quit and relaunch, then reverted (verified via `git diff` showing zero difference from the prior commit — a clean revert, nothing left behind). This rules out a stale build/cache/Metro/wrong-target explanation for earlier passes' fixes not appearing on-device; whatever gap remains between "committed and verified by code" and "what's on screen" is a real code-level question, not a pipeline one.

**Found while investigating exactly what to expect at each step of a requested on-device walkthrough:** Going Quiet's picker (`GroupPicker.tsx`) never wires `hasSentThisSession` into its chips at all, and structurally can't support the "select, send, come back and reselect" pattern the other four screens have — Going Quiet is a single whole-screen Send (one `sentAt`, not per-Circle), and once it fires, the interactive chip row is replaced entirely by static, non-interactive "✓ CircleName" text rows (`circleSentRow`), not an `AdaptiveCircleChip` in any state. Reported directly rather than let a walkthrough be run against a screen that was never built to support what was being tested.

**"+" (New Circle) pinned outside the horizontal scroll, always visible; "All" moved to be the scrollable row's first item instead.** Checked all five `AdaptiveCircleChip` call sites before touching any of them: only `GroupPicker.tsx` actually has a "+" chip. Manage Circles uses a header-level "+" button (a separate, deliberate pattern from earlier this session); Reconnect, Taking Time's update, and Conversations' Quick message never had one, since none of them create Circles from their own picker. Applied the change to the one screen it actually applies to, rather than touching the other four or inventing a "+" where none exists.

## 2026-08-10 — Scroll-to-reach-Send fixed structurally; "Taking time" heading corrected

**Confirmed the earlier scroll-to-reach-button fix was genuinely still present** in `create/done.tsx` (`paddingTop: theme.spacing.xxl`, `HoldMark size={64}`) before looking any further — not reverted, not a stale-build issue (already ruled out by the prior entry's diagnostic). The real cause was the fix's own category: fixed padding/size tuning can't hold under every condition (a longer subtitle, a larger accessibility text size can still push the button below the fold), so it was always going to reopen.

**Replaced with a structural fix.** `Screen.tsx` gained an optional `footer` prop — a `ReactNode` rendered fixed below the internal `ScrollView`, inside the same `SafeAreaView`, so a primary action there is never part of the scrollable region at all. Applied to the three short, fixed-content arrival/completion screens: `create/done.tsx`, `return/done.tsx`, `return/transition.tsx` (all three moved their `PrimaryButton` out of the scroll and into `footer`, dropping the now-unneeded `justifyContent: "space-between"`). Deliberately **not** applied to `create/people.tsx`, `return/reconnect.tsx`, `return/update.tsx` — these have genuinely variable content (a growing Circle list, expanded message boxes) where scrolling to reach Send is expected, and pinning a footer there risks covering content or fighting the keyboard-inset handling `Screen.tsx` already does for compose screens.

**"Taking time" heading bumped** in `(tabs)/index.tsx` from `{fontSize:17, fontWeight:"600"}` to match `circleLabel` exactly (`{fontSize:22, lineHeight:27, fontWeight:"700", textAlign:"center"}`) — the same Large-tier heading style already used for Home's circle header, per hold-book's `05-design-system/02-colour-and-typography.md`, reused rather than inventing a new size.

**Also investigated and reported (no code change): Home's gating.** "Send an update" is hidden until the calendar day changes from `openPeriod.startedAt` (`!isSameCalendarDay(openPeriod.startedAt, Date.now())`) — a day-boundary gate, not a duration, so it can appear the very next morning even after a short Hold. "Reconnect" itself (the main circle tap target while `homeState === "taking-time"`) has no gate at all — always available immediately. Both confirmed by direct reading of `(tabs)/index.tsx`, not assumed.

`tsc --noEmit` and `vitest run` (27/27) both pass.

## 2026-08-10 — Going Quiet's Send reworked: per-Circle and repeatable, not single-shot

**What changed structurally.** The single screen-level `sentAt: number | null` flag is gone. In its place:
- `selectedCircleIds: Set<string>` (local state) — which drafted Circles are currently expanded/included in the next Send. A new Circle drafted via `GroupPicker` joins this set automatically (mirrors the old "every selected Circle has a visible card" default); a Circle that's been sent and then deliberately deselected is left alone, not re-added.
- `sentCircleIds` (derived, not stored) — computed from the still-open `HoldPeriod`'s existing `sendChannels` field, filtered to keys that match a drafted Circle's id. `recordSendChannel` already wrote this on every real group send; no new persisted field was needed, the same derived-from-existing-durable-state approach used for Library's Quick message.
- A new `AdaptiveCircleChip` row above the per-Circle cards, one chip per drafted Circle, `isSelected`/`hasSentThisSession` wired exactly as on Reconnect/`update.tsx`. Only selected Circles render their expanded card (message box, recipient personalisation, safeguarding banner); a sent-and-deselected Circle collapses to a compact `✓ CircleName` chip that's still tappable — tapping it re-selects (reopens its card) so another message can go to that same Circle. Deselecting a sent chip without sending falls back to the sent look, not blank, by construction (the sent-derived signal is never touched by a selection toggle).
- `send()` now only sends the currently-*selected* drafts (`selectedDrafts`, a filter of `circleDrafts`), not every drafted Circle. The `HoldPeriod` is created once, on this session's first Send, and reused (via `period?.id ??`) on every later Send — the old code called `startHoldPeriod` unconditionally on every Send, which was harmless only because Send used to be reachable exactly once; made repeatable, that would have silently orphaned the first period on a second Send.
- The "want to send anyone something more personal?" / pending-Circle-confirm / OOO / Wider World / "Done" sequence now gates on `hasSentAnything` (`sentCircleIds.length > 0`) instead of the removed `sentAt !== null` — reachable after the *first* send, not after every drafted Circle has been sent. The personal-note re-offer (`excludedNotRemoved`) is now scoped to excluded people whose Circle is actually in `sentCircleIds`, not the whole audience — a Circle that hasn't been sent yet has no one to personalise about.

**Confirmed, not assumed, what happens in each state:**
- **Nothing sent yet:** only the picker (chip row + expanded cards) and a Send button, disabled until at least one selected Circle has a non-empty message. No confirmation text, no Done — matches before.
- **Partial send:** sent Circles collapse to a tappable sent chip; unsent/never-touched Circles' cards stay open below the chip row; Send stays enabled for whatever's currently selected; the confirmation line and Done sequence appear, reachable immediately — Done does not require every Circle to be sent.
- **All Circles sent:** looks the same as partial, just every chip is in the sent/reselectable state — there was never a distinct "fully sent" gate, in this model or the one it replaces.

**Explicitly not touched this pass**, per direct scope: the combination-keyed shared-message-per-group behaviour, the Personalise step, and OOO/Wider World placement — these remain as they were, follow-on pieces of the same broader redesign.

**Known, deliberately unaddressed edge case:** if a new Circle is added via `GroupPicker` *after* the first Send has already created the `HoldPeriod`, that Circle's contacts won't be reflected in `period.audienceCircles` (fixed at first-Send time) until the period closes — a narrow gap in Reconnect's later coverage total, not a regression (the old single-shot code had no path to reach this state at all, since Send only ever fired once). Flagged rather than fixed, to stay inside this pass's explicit scope.

`tsc --noEmit` and `vitest run` (27/27) both pass. **Still flagged, not resolved:** no on-device verification possible from this session — needs a walkthrough on the device, the same limitation as every pass before it.

## 2026-08-10 — Three post-send fixes: in-place sent chips, pending-Circle prompt moved to Reconnect, "Sent" line removed

**1. Sent-circle "new line" bug.** Root cause: the previous rework had added a genuinely *separate* `ScrollView` chip row in `create/people.tsx`, directly below `GroupPicker`'s own existing row, to represent send-selection. This was a deliberate choice at the time (audience-removal and resend-reselect seemed like they needed different data-preservation semantics — `toggleGroup`'s deselect branch deletes the Circle's draft entirely, which is correct for "remove from audience" but wrong for "temporarily collapse a sent card"), not an oversight. But it diverged from every other screen using this chip pattern, which has exactly one row, and read on-device as a checkmark spawning on a new line rather than an in-place colour change.

**Fixed by merging into one row.** `GroupPicker` now takes `sentCircleIds`/`reselectedCircleIds`/`onToggleReselected` props. A not-yet-sent Circle's chip keeps its exact original meaning and look — tap adds/removes it from the audience, card always visible once added, no behaviour change at all. Only once a Circle has actually been sent does its chip's tap meaning change, to "toggle reselected" (reopen its card for a further/different send) instead of "remove from audience" — removing a sent Circle wouldn't undo the message anyway, so the old meaning stops making sense at exactly that point, not before. `toggleAll` ("All") now skips already-sent Circles for the same reason. `create/people.tsx`'s separate chip-row `ScrollView` is gone entirely; the screen's local `selectedCircleIds` state (`isSelected` = expanded/next-Send) is replaced by `reselectedCircleIds` (only meaningful for already-sent Circles), and card visibility is now `!sentCircleIds.includes(id) || reselectedCircleIds.has(id)` — unconditionally visible until sent, then only visible when reselected.

**2. "Add [X] to [Circle] permanently?" no longer fires during Going Quiet.** Per direct instruction: this was always meant to be a Reconnect-only prompt (asking someone to decide Circle permanence in the moment of going quiet contradicts the app's low-capacity principles), though this apparently was never actually written into hold-book — corrected now, not just in code. Removed `pendingNewCircles`/`resolvedPendingCircles` state and the whole prompt block from `create/people.tsx`. Moved to `return/reconnect.tsx`'s "everyone's been reached" gate, ahead of "want to reply to anyone properly?". No new persisted list was needed: a pending Circle's id already carries a `pending-` prefix (now a shared `PENDING_CIRCLE_ID_PREFIX` constant exported from `GroupPicker.tsx`), so Reconnect finds unresolved ones directly by filtering `period.audienceCircles`. Added `HoldPeriod.resolvedPendingCircleIds` and `holdHistoryService.markPendingCircleResolved(periodId, circleId)` (explicit `periodId`, not `OPEN_KEY` — this fires after the period has already closed) so a force-quit mid-Reconnect doesn't re-ask.

**3. Removed the "Sent. You've communicated to everyone who needs to know." line** from `create/people.tsx` entirely, per direct instruction — a deliberate design decision, not a bug fix. The per-Circle checkmark + colour transition already satisfies "never rely on colour alone" at the individual level; the sentence was also no longer accurate once sending became partial/repeatable rather than one-shot. The positive "you've done what's needed" feeling is deferred to the Transition screen — explicitly **not** built in this pass, per instruction, since that's separate follow-on work.

**Flagged back, not fixed, per direct request:** with the confirmation line gone, the actual on-screen order in `create/people.tsx` (top to bottom) is: `StepHeader` → `GroupPicker` (audience/send chip row) → visible per-Circle cards → **Send** → (once anything's been sent) "want to send anyone something more personal?" prompt → OOO/status disclosure (collapsed by default) → **Done**. OOO/status is not its own separate screen or step — it's stacked directly beneath Send/the picker on the same scrollable screen, gated behind `hasSentAnything`. This isn't a new regression: it's exactly what the 2026-07-29 "single scrollable screen, no separate Review step" decision already specified. Whether that still reads right now that the confirmation line (which gave that stacked area some visual weight/pause) is gone is a real product question, not resolved here.

`tsc --noEmit` and `vitest run` (27/27) both pass. **Still flagged, not resolved:** no on-device verification possible from this session.

## 2026-08-10 — Split Send buttons: compact icon-only for repeated actions, restyled full-width pill for one-time completion

**Audit (requested before any change), every Send-style button in the app:**

REPEATED/PER-ITEM (multiple taps in one sitting, one per circle/person):
- `create/people.tsx` — Going Quiet's main Send (per visible/selected Circle card)
- `create/people.tsx` — Going Quiet's personal-note Send (per excluded recipient)
- `return/update.tsx` — Taking Time's "Send an update" (explicitly "as many times as you like")
- `return/reconnect.tsx` — Reconnect's Send (repeatable across partial sends until coverage completes)
- `(tabs)/library.tsx` — Quick message's per-Circle bulk Send, per-excluded-member individual Send, per-ungrouped-person individual Send (three separate call sites)
- `(tabs)/library.tsx`'s `PersonaliseAccordion` — "Send now" (one per person, repeated across a Personalise sitting — a judgment call, not explicitly named in the request, included for consistency with the same repeated-sequential shape)

ONE-TIME COMPLETION (tapped once per visit, ends/advances the whole flow) — unaffected by the compact redesign, restyled per below:
- `create/done.tsx` — "Begin Taking Time"
- `return/done.tsx` — "Done" (ends Reconnect)
- `return/transition.tsx` — "Continue"
- `create/people.tsx` — "Done" (Going Quiet's own end-of-flow, distinct from the repeated Sends above it on the same screen)
- `welcome.tsx` — "Get started"
- `settings/circle/index.tsx` — "Update circle" (not a Send action at all, but shares `PrimaryButton` and so inherits the restyle below automatically — flagged as a side effect, not a defect)

**Repeated buttons → new `src/components/CompactSendButton.tsx`.** Icon-only, standard paper-plane glyph via `@expo/vector-icons`' `Ionicons name="send"` (added as a new dependency — Expo's own first-party icon package, not previously installed anywhere in the app, which currently renders all other iconography as plain Unicode glyphs like "▲"/"✓"). Same `colors.primary`/`colors.onPrimary` fill as before. Fixed 44pt (iOS) / 48pt (Android) tap target — the exact same accessible floor as `AdaptiveCircleChip`'s `STANDARD_CHIP_DIAMETER` — regardless of how compact the icon reads visually. `accessibilityLabel` defaults to `"Send"`, overridden per call site to something more specific where useful (`"Send to Book Club"`, `"Send to Priya"`) since an icon-only button has no visible text label to do that work. Replaced the old `PrimaryButton`/`SecondaryButton` "Send" at every REPEATED site above; removed now-dead `PrimaryButton` imports where nothing else in that file still used it (`return/update.tsx`, `(tabs)/library.tsx`).

**One numeric-count regression, flagged, not silently dropped:** Quick message's per-Circle bulk Send used to show `"Send (3)"` — the count is now only reachable via the button's `accessibilityLabel` ("Send to 3 people in Book Club"), not visible on-screen, since the icon-only button has no room for a number. Kept as the simplest correct move for this pass; a small badge is an easy follow-up if the visible count turns out to matter.

**One-time completion buttons (`PrimaryButton`) restyled, not replaced:** `borderRadius` changed from `theme.radius.md` (16, a rounded rectangle) to `theme.radius.pill` (999) — matching every other "strong"/filled element in the app (Close Circle's chip, sent-state chips, every pill-shaped selectable), all of which already used the pill radius; `PrimaryButton` was the one inconsistency reading as a dated, boxier shape. Added `marginHorizontal: theme.spacing.lg` — real side margins on top of its container's own existing padding (Screen's `content`/`footer` already carry `paddingHorizontal: theme.spacing.lg`), so the button reads as an object placed on the screen rather than a bar filling it. Fill/text colour unchanged — `colors.primary`/`colors.onPrimary` is already exactly the "Strong/primary green fill" token named in hold-book's own colour table, so no colour change was needed or made. **No icon added to `PrimaryButton`, deliberately** — per direct instruction, "Begin Taking Time" (and every other one-time completion button) stays text-only: this is the app's one restful completion moment, not an achievement, and an icon here would collide with symbols already meaningful elsewhere (Circle iconography, the moon-cycle overlay) and add action-energy a calm screen isn't meant to have.

**Not done — flagged, not fabricated:** the compact button's exact size and the corrected `AdaptiveCircleChip` diameter were both requested to be derived from measuring reference screenshots (Instagram's chat-send button and story-row avatars). Real screenshots did arrive mid-session, but this environment has no pixel-measurement tooling (no image-analysis execution, no confirmed access to the screenshot's true native resolution vs. its displayed size) — a literal "measured in pixels, converted to points" figure isn't something that can be honestly produced here, only a rough eyeballed proportion dressed up as precision, which is exactly what was asked not to do. `CompactSendButton` currently uses the same 44pt/48pt accessible floor as the Circle chips as a safe, already-established placeholder — not the final Instagram-matched size. `STANDARD_CHIP_DIAMETER` is unchanged. Both remain open pending either a genuine measurement or a number given directly.

`tsc --noEmit` and `vitest run` (27/27) both pass. **Still flagged, not resolved:** no on-device verification possible from this session — this is a real visual/layout change (row alignment, icon centring, spacing around the now-compact buttons) that needs a look before being treated as done, more than most changes this session.

## 2026-08-10 — Chip diameter to 64pt/68dp (given directly); Quick message's reach count restored as visible text

**1. `STANDARD_CHIP_DIAMETER`: 44pt/48pt → 64pt (iOS) / 68dp (Android).** Final numbers given directly, not derived from measuring the reference screenshot — this environment has no pixel-measurement tooling (no image-analysis execution, no confirmed access to a screenshot's true native resolution), so a literal "measured from the photo" figure was never something that could be honestly produced here; flagged plainly rather than faked. `CompactSendButton`'s own tap target is unchanged at 44pt/48pt (the bare accessibility floor) — the two are now deliberately different sizes, not a coincidence to reconcile.

**Recalculated circle-vs-pill fit table at the new size** (same estimated-metrics caveat as every prior pass: 14pt/weight 600, San Francisco-equivalent, not an on-device measurement). `CIRCLE_FIT_PADDING` (`theme.spacing.sm` = 10) is unchanged, so available inner width is `STANDARD_CHIP_DIAMETER - 20`:

| Platform | Available width |
|---|---|
| iOS | 64 − 20 = 44pt |
| Android | 68 − 20 = 48dp |

| Label | Est. text width | iOS (44pt avail.) | Android (48dp avail.) |
|---|---|---|---|
| "D" | ~9pt | circle | circle |
| "+" | ~9–10pt | circle | circle |
| "All" | ~22pt | circle | circle |
| "Work" | ~34–36pt | circle | circle |
| "Close" | ~37pt | circle | circle |
| "Friends" | ~48–52pt | pill | pill (borderline — estimate sits at/just past the 48dp available width) |
| "Book Club" | ~75pt | pill | pill |

The main change from the previous (44pt/48pt) table: "Close" now fits as a circle on both platforms — it didn't at the smaller size. "Friends" and "Book Club" still don't fit; "D"/"+"/"All" were already circles and stay circles.

**2. Quick message's per-Circle bulk Send regains its visible reach count**, previously dropped to `accessibilityLabel`-only when this button was compacted. Not a numeral-in-a-circle badge (built, then explicitly rejected) — a badge would visually compete with the Circle chips on the same screen and risk reading as a notification rather than a count. Instead, `CompactSendButton` gained an optional `label` prop: when set, the button grows from a plain icon-only circle into an icon+text pill (same fixed height, width grows to fit — the same shape language `AdaptiveCircleChip` already uses for circle-vs-pill). Text: `"Send (N people)"` / `"Send (1 person)"`, singular/plural handled.

**Corrected "Circles" → "people."** The request's own phrasing ("Send (3 Circles)") didn't match what this button actually does: `sendCircle(section)` is scoped to exactly one Circle's `section.people`, and already shows its own confirmation `Alert` reading `"Reaches {recipientCount} people"` before sending — direct evidence the count was always people within one Circle, never a count of Circles. Implemented as "people" and flagged the correction rather than shipping copy that would have misdescribed the button.

**Legibility at the smallest supported text size, reasoned rather than rendered:** the label is `numberOfLines={1}` at 15pt/weight 600 inside a fixed-height pill that grows in width to fit; at iOS's smallest Dynamic Type step (~80% of default, so ~12pt effective), a short phrase like "Send (3 people)" stays well within normal legible range for a filled button — this is a reasoning-based check, not an on-device render, and is flagged as such rather than claimed as confirmed.

`tsc --noEmit` and `vitest run` (27/27) both pass. **Still flagged, not resolved:** no on-device verification possible from this session — chip size, the new pill-shaped Send button, and text-scale legibility all need a look before being called done.

## 2026-08-10 — Unified text entry app-wide onto one docked input bar; Amend with AI moved into it

**New dependencies:** `react-native-keyboard-controller` (Expo's own documented recommendation for anything beyond basic `KeyboardAvoidingView`; `InputAccessoryView` — RN's native "dock above keyboard" API — is already confirmed broken here, per `Screen.tsx`'s existing comment about the New Architecture) and `expo-speech-recognition` (on-device dictation, wraps iOS's `SFSpeechRecognizer`/Android's `SpeechRecognizer`). `app/_layout.tsx` now wraps the tree in `KeyboardProvider`. `app.json` gained the speech-recognition plugin with Hold-toned permission copy.

**New shared components:**
- `src/components/DockedInputBar.tsx` — the one input surface, rendered via `KeyboardStickyView` so it docks directly above the keyboard. Optional `aiAmend` prop (`{surface, context, initialPrompt}`) shows a Hold+-gated AI-assist icon that expands into a compact inline prompt+Generate row, built into the bar itself. `DictationMicButton` sits inline too — on-device-only (`requiresOnDeviceRecognition: true`).
- `src/components/DockedFieldPreview.tsx` — the on-page trigger every field renders when not active: empty shows a placeholder, non-empty shows the current value + "Edit." Tapping either activates that field in the screen's one `DockedInputBar`.
- `src/hooks/useDockedAiAmend.ts` — the Hold+-gated generate/prompt/status state machine, extracted from the now-deleted `AmendWithAI.tsx` so `DockedInputBar` can own it directly. Preserves the original `initialPrompt` auto-open behavior (AI memory Layer 2's "Use it" suggestion).
- `Screen.tsx` gained a `dockedInput?: ReactNode` prop, rendered as a sibling to the scrollable content (same tier as `footer`), so `KeyboardStickyView` isn't nested inside a `ScrollView`.

**Architecture: one `DockedInputBar` per screen, not per field.** Each screen owns an `activeField` key (a string-literal union, e.g. `` `circle:${string}` | `recipient:${string}` | "new-circle" ``) and a resolver that maps the active key to that field's `value`/`setValue`/placeholder/`aiAmend` config — mirroring the pattern used for `AdaptiveCircleChip`'s multi-instance state earlier this session. Where a field's state lives inside a nested component instance rather than the screen itself (`(tabs)/library.tsx`'s `PersonaliseAccordion`, one per person), that component hands the parent its own bound `changeText` closure at activation time (so autosave-to-storage, which needs that instance's local `friendMessage`/`sentAt`, keeps firing on every keystroke typed through the shared bar) rather than the parent guessing at a raw setter.

**Full audit, 19 sites, all migrated except one:**
- Going Quiet (`create/people.tsx`): main per-Circle message, `RecipientPersonalisation`'s individual instant message, personal-note re-offer, `GroupPicker`'s new-Circle name.
- Reconnect (`return/reconnect.tsx`), Taking Time's update (`return/update.tsx`): each screen's one message field.
- Manage Circles (`settings/circle/index.tsx`): new-Circle name.
- Conversations/Library (`(tabs)/library.tsx`): Quick message's per-Circle shared box, per-excluded-member and per-ungrouped-person individual message, new-Circle name, saved-template editing, Personalise's "Your reply" (`PersonaliseAccordion`, lifted `draft`/`style` to the screen so the shared bar stays correctly controlled).
- `EmailOutOfOffice.tsx`: shared OOO message, per-account message, account label (confirmed in scope directly — "no field too small," reversing an earlier judgment call to leave it as a plain field).
- `WiderWorldStatus.tsx`: status line.

**One deliberate exception, per direct (and later reconfirmed) instruction:** `library.tsx`'s "Message they sent" (`PersonaliseAccordion`'s pasted-in friend-message field) stays an in-page `TextInput` — reference content pasted once, not actively composed, needs to stay visible for scroll-back reference while the reply is written separately. Its existing settle-on-blur/"Edit"-to-reopen behavior already matched the requested "read-only after confirmed" shape, so no logic change was needed there — only confirmed correct and left alone.

**`GroupPicker.tsx`'s suggestion chips** ("Friends," "Work," "Book Club") lost their "+" prefix — name only, shown until typing starts (a non-empty name hides the row, matching "chips replaced by free typing").

**Amend with AI (Hold+) supersession:** the standalone `AmendWithAI.tsx` panel (a link below the message box expanding into its own prompt/generate section) is deleted entirely, replaced by the trigger built into `DockedInputBar`. `AiSurface`/`PromptSurface` gained three new values — `email-ooo`, `wider-world-status`, `template` — so OOO/status/Library-template editing get AI-amend for the first time; the worker (`worker/src/prompts.ts`, `worker/src/index.ts`) was updated to match and **needs a redeploy** to take effect against the live proxy, same as every other worker-prompt change this session.

`tsc --noEmit` and `vitest run` (27/27) both pass. **Still flagged, not resolved, more than most changes this session:** this touches the interaction shape of nearly every text-entry point in the app, with no on-device or simulator verification possible from here — keyboard docking behavior, the AI-amend inline prompt's layout, dictation permissions, and the read-only "message they sent" exception specifically all need a real device check before being treated as done.

## 2026-08-11 — Docked-bar/circle-picker refinement pass (continuation of the docked-bar/chip work, not a new topic)

Based on on-device screenshots. Seven items, one (chip diameter) presented as options first per direct instruction, the rest implemented directly.

**1. Dropdown arrow, chip diameter grown again.** `STANDARD_CHIP_DIAMETER`: 64pt/68dp → **72pt (iOS) / 76dp (Android)**, the chosen option among three presented (overlay chevron outside the fit check; grow the diameter, same technique Manage Circles already uses; shrink font for arrow-chips only). `GroupPicker.tsx`'s named-Circle chips now append `" ▼"` (collapsed) / `" ▲"` (expanded) into the label — exactly Manage Circles' `renderPill` technique, previously never applied to Going Quiet's own picker at all (a genuine gap, not a new feature — hold-book's "Circle picker layout" section already specified selected pills carrying a down-arrow). A sent-and-collapsed chip keeps its `✓ CircleName` look instead, no arrow — the two signals don't stack. Recalculated fit at the new diameter (available width = diameter − 20 padding = 52pt iOS / 56dp Android): "Close ▼" (~52pt) is a tight, close-to-the-line fit at exactly the iOS threshold — flagged, not a comfortable margin, pending on-device confirmation; "Work ▼", "All ▼", "D ▼" fit comfortably; "Friends ▼"/"Book Club ▼" stay pills, as they already did without the arrow.

**2 + 3. Simplified "+" flow; suggestions moved above the docked bar.** `GroupPicker.tsx`'s `addCircle` function — pick a contact, build the pending Circle, call `onToggle` — is **lifted up to `create/people.tsx`** as `submitNewCircleName`, since the docked bar's new suggestion chips (rendered above the keyboard via `DockedInputBar`, not inside `GroupPicker`'s own position in the scrollable page) and `GroupPicker`'s own "Add" button both need to trigger the identical submission. `GroupPicker` lost its local `creating` state (now just uses the `isNamingActive` prop directly), its `addCircle` function, its inline suggestion row, and the `DockedFieldPreview` that required an extra tap to open the bar — replaced by: tapping "+" calls `onActivateNaming()` immediately (docked bar opens, focused, autoFocus), and a small "Cancel"/"Add" row appears in its place. `DockedInputBar` gained an optional `suggestions?: {label, onPress}[]` prop, rendered as its own row directly above the main input line, visible only while the field is empty (mirrors the old inline-row visibility rule) — `create/people.tsx` passes `SUGGESTED_CIRCLES.map(name => ({label: name, onPress: () => submitNewCircleName(name)}))` when `activeField === "new-circle"`. `GroupPicker`'s now-unused `onPendingContact` prop and the `PendingNewCircle` exported type are removed entirely (confirmed no other importers first).

**4. Dictation mic icon enlarged** — `DictationMicButton`'s `Ionicons` size 20 → 26, a default bump since no exact target was given; flagged as adjustable.

**5. Docked bar's top border** — `borderTopWidth` 1 → 3, `borderTopColor` `colors.border` (neutral grey) → `colors.primary` (the app's own Strong/primary green), no new colour invented.

**6. Blended fill.** New `src/utils/colorMix.ts` — a small, dependency-free `mixColors(hexA, hexB, ratio)` linear-RGB blend, computed live per active palette rather than hardcoding four hex constants for light/dark × normal/quiet. `DockedInputBar`'s background (and the new suggestion chips') changed from flat `colors.surface`/`colors.surfaceStrong` to `mixColors(colors.surfaceStrong, colors.border, 0.6)` — a 60/40 blend of the pale chip green and the app's own neutral border-grey token. Computed values: light-normal `#c8d1c9`, light-quiet `#d1ccb3`, dark-normal `#3a4039`, dark-quiet `#413a2a`. Suggestion chips keep a `colors.primary` outline on top of the same fill so they stay visually distinct from the bar behind them rather than disappearing into an identical background. **Not resolved, flagged:** whether there's a literal pixel gap between the bar and the keyboard (as opposed to a colour-contrast issue reading as one) — found no explicit margin/offset in `DockedInputBar`'s styles or `KeyboardStickyView`'s usage that would cause one; the blended fill may resolve the perceived gap on its own, but this needs on-device confirmation specifically, and `KeyboardStickyView`'s own offset behaviour wasn't independently re-verified against its docs this pass.

**7. One confirm icon, no exceptions.** Mid-thread, a context-aware "checkmark for name/label fields, paper-plane for messages" variant was proposed, then explicitly corrected before any code was written: paper-plane only, everywhere, always. Confirmed `DockedInputBar`'s own "Done" button — which still had a leftover `Ionicons name="checkmark"` from before this session's docked-bar work even existed — now uses `name="send"` with the same optical-centring nudge `CompactSendButton` already uses for its own paper-plane. Grepped the whole app for any other `"checkmark"` icon usage: none found.

## 2026-08-11 — Second same-day refinement pass (14 items): circle/arrow split, diameter to 90/95, Close de-special-cased, docked-bar pill redesign, real keyboard-flush fix

Consolidated batch from further on-device testing of the pass immediately above. Several items correct that same pass; logged as corrections, not as if it never happened. `tsc --noEmit` and `vitest run` (27/27) both pass after every change below.

**1. Flush-to-keyboard gap — real fix, not colour.** Root cause: `Screen.tsx` rendered `dockedInput` (the `KeyboardStickyView`) as a child of its own bottom-inset `SafeAreaView`, so the bar's keyboard-closed resting position sat above the true screen bottom by the home-indicator inset. `KeyboardStickyView` translates its child by exactly the keyboard's own pixel height (`translateY: height.value`, confirmed by reading `react-native-keyboard-controller`'s own source, not assumed) — that math has no way to know about an inset it isn't told about, so the bar stopped short of the keyboard by exactly that inset once open. Fixed by restructuring `Screen.tsx`: `dockedInput` now renders as a sibling of the inset `SafeAreaView`, inside a plain outer `View` that reaches the screen's true edges. `DockedInputBar`'s own inner `SafeAreaView` also dropped its `"bottom"` edge (kept `"left"/"right"`) — it's redundant now, and was previously double-applying the same inset a second time regardless of keyboard state. Reasoned from the library's own implementation; not verified against a live device measurement, since this session has none.

**2. Docked bar redesigned — Instagram-style pill, corrected colour spec.** Mic and paper-plane icons moved inside the text field's own rounded pill as one continuous shape (new `pill`/`input` styles in `DockedInputBar.tsx`, replacing the prior three-siblings-in-a-row `row`); only the Amend-with-AI toggle stays outside it. Inside the pill: kept the existing `mixColors(colors.surfaceStrong, colors.border, 0.6)` blend, but text colour changed from the prior pass's `colors.text` framing being ambiguous to explicitly `colors.text` (never a fixed white) — computed WCAG contrast against the theme's own defined hex values: light-normal (`#c8d1c9` fill / `#242825` text) ≈ **9.55:1**; dark-normal (`#3a4039` fill / `#edefe8` text) ≈ **9.19:1** — both comfortably past AA's 4.5:1, calculated from theme constants, not measured on-device. Outside the pill: `barFill = mixColors(pillFill, keyboardBackdrop, 0.3)`, blending mostly toward a new reasoned approximation of the system keyboard's own colour (`KEYBOARD_BACKDROP_LIGHT = "#D1D3D6"`, `KEYBOARD_BACKDROP_DARK = "#2C2C2E"` — not sampled from a real device, since app code can't read the live keyboard's pixels; flagged for on-device confirmation) rather than the app's own palette tokens, correcting the immediately-prior pass's "blend toward the app's border/background" approach.

**3. Suggestion chips — plain text, not circles.** `DockedInputBar`'s `suggestionChip`/`suggestionText` circle-styled Pressables replaced with a plain-text row (`suggestionItem`/`suggestionDivider`/`suggestionText`), words separated by a `StyleSheet.hairlineWidth` vertical divider, at 16pt — matching the input's own font size and the documented 15-17pt Compact-text range.

**4. Chip diameter 72/76 → 90/95pt, name-fit audit.** `STANDARD_CHIP_DIAMETER` in `AdaptiveCircleChip.tsx`: `Platform.OS === "android" ? 95 : 90` (from 76/72), final figure given directly. `CIRCLE_FIT_PADDING` unchanged at `theme.spacing.sm` (10pt), so available circle width = 70pt (iOS) / 75pt (Android). Estimated text widths (≈0.58 × fontSize per character, an SF Pro Semibold approximation — **not device-rendered**, this session has no simulator/measurement tooling): at 14pt default size, "Close" (~41pt), "Friends" (~57pt), "Work" (~32pt), "All" (~24pt), and short user-created examples ("Family" ~49pt, "Mum" ~24pt) fit comfortably inside the circle threshold on both platforms; "Book Club" (~73pt) sits right at/over the line — pill on iOS, borderline-circle on Android by this estimate, a genuine close call, not confidently resolved; a stress-test long name ("College Friends," ~122pt) clearly exceeds it and renders as a pill, as designed. At the largest Dynamic Type accessibility size (~43pt effective, ≈3.1× base, per Apple's own Dynamic Type scaling table), every name tested — including "Close" and "All" — becomes a pill; this is the intended circle-to-pill degradation, not a bug. No name is ever shrunk or truncated at any size: `numberOfLines={1}` combined with an unconstrained pill width and a chip height (90/95pt) taller than even the largest accessibility line height guarantees this structurally, confirmed by reading the component's own layout logic, not by rendering the actual text.

**5. Circle/arrow split.** `GroupPicker.tsx`'s named-Circle row is now two independent `Pressable`s per Circle: the `AdaptiveCircleChip` itself (label is just the name now, arrow no longer appended — send-selection semantics unchanged) and a separate small arrow `Pressable` beside it (`▼`/`▲`, `hitSlop={8}`, own `accessibilityRole`/`accessibilityState`), driving a new `expandedCircleIds` state lifted to `create/people.tsx`. `visibleDrafts` (the card-render set) is now `sentCircleIds ∩ (reselected ∪ expanded)`-shaped — a sent Circle's card can be opened via its arrow for viewing/editing without that alone counting as a reselect-to-resend. `send()`/`canSend` were re-pointed at a new, narrower `draftsToSend` (reselected-or-never-sent only) so a merely-expanded sent Circle is never accidentally resent. Confirmed this split doesn't apply to Manage Circles' own chip (`settings/circle/index.tsx`) — its chip only ever had one function (expand/collapse the member card), nothing to split.

**6. "Manage your Circles" removed from Going Quiet.** Grepped the whole app first: `GroupPicker.tsx` was its only render site, nothing else depended on it. The `<Link>` block, and the styles it alone used, are deleted.

**7. Provisional-circle audience sync — real bug found and fixed.** Checked `return/reconnect.tsx`'s `pendingCircles` filtering first: it does **not** gate on `sendChannels`/sent-state, so the literal question ("does the prompt only fire on send?") was already "no." But `period.audienceCircles` is only ever written once, at the very first `startHoldPeriod()` call — a Circle added to the selection *after* that first Send, with no further Send happening afterward, would never be synced into it at all, meaning Reconnect's "add permanently?" prompt would never fire for that person and they'd silently drop out of every circle with no signal anywhere. Fixed with a new `syncAudience()` in `holdHistoryService.ts` (mirrors `recordPostSendChoices`'s read-merge-write pattern against `OPEN_KEY`), called from `create/people.tsx`'s `finish()` — re-syncs `recipients`/`audienceCircles` to the full current selection at Going Quiet's "Done" step, independent of whether another Send happens first.

**8. Old "+"-flow Add/Cancel buttons removed.** `GroupPicker.tsx`'s inline "Cancel"/"Add" `Pressable`s and their styles are deleted. Paper-plane (the docked bar's own `onDone`) commits; a new `onDismiss` prop on `DockedInputBar` (default: `onDone`) is wired to a `Keyboard.addListener("keyboardDidHide", ...)` effect, so tap-outside (which already dismisses the keyboard app-wide via `Screen.tsx`'s existing `TouchableWithoutFeedback`) now also closes the bar without committing, for every field on every screen, not just this one. `create/people.tsx` gives `"new-circle"` its own `onDismiss` (clears the draft name without submitting) and its own `onDone` (submits via `submitNewCircleName`); every other field keeps the prior "just close" default.

**9. "+" final spec.** Glyph-only label (`"+"`, no baked-in text), `labelFontSize={28}` — larger than the small arrow glyph beside named chips, since it has no competing label text sharing the circle. Restructured into a small vertical stack (`newCircleStack`): the chip, then a "New Circle" `Text` caption beneath it with a small gap, visible only while active.

**10. Docked bar top border removed; top corners rounded instead.** The prior pass's `borderTopWidth: 3` / `borderTopColor: colors.primary` read as a jarring, heavy line on-device — removed entirely, no border of any kind. Replaced with `borderTopLeftRadius`/`borderTopRightRadius: theme.radius.lg` (24pt) — a reasoned, deliberate choice, not a measured one (the real system keyboard's own corner curvature, if any, can't be read from app code); flagged for on-device confirmation.

**11 + 14. "+" active-state ring — real bug found and fixed.** Confirmed by direct code read: `AdaptiveCircleChip`'s selected-ring style was applied via `isSelected && !outline && styles.chipSelected` — structurally, an `outline` chip (i.e. "+") could **never** show a ring, regardless of `isSelected`, exactly matching the on-device report that "+"'s active state was imperceptible. Fixed with a `selectedRingStyle` ternary (`outline ? styles.chipSelectedOutline : styles.chipSelected`) and a new `chipSelectedOutline` style: `backgroundColor: colors.surfaceStrong, borderWidth: 3, borderColor: colors.primary` — a distinct filled-tint-plus-3px-border treatment, not the same as a selected Circle's dark-green fill. `GroupPicker.tsx` now passes `isSelected={isNamingActive}` to the "+" chip, so tapping "+" visibly activates it; tapping again, or tapping outside, closes it via the same generic dismiss path as item 8.

**12. "+" and "All" excluded from the sent-fill system — confirmed, no code change needed.** Neither has a real send target of that kind; `AdaptiveCircleChip`'s `hasSentThisSession` is never passed for either in `GroupPicker.tsx`.

**13. Close de-special-cased.** Removed the `isPrimary` prop from `AdaptiveCircleChip` entirely (and the `chipPrimary`/`labelTextPrimary` styles it drove) — Close now uses the exact same default/selected/sent treatment as every other Circle, no colour distinctiveness. Added a new `labelBold?: boolean` prop instead, wired to a `fontWeight: "800"` style; both `GroupPicker.tsx` and `settings/circle/index.tsx`'s `renderPill` now pass `labelBold={group.isCloseCircle}`. Close's fixed-first position (unchanged) remains the primary identification mechanism; the bold weight is a secondary cue only, no colour reintroduced. This reverses part of a 2026-07 decision (Close's "stronger fill"), not a same-day one — flagged as such in `hold-book`'s decision log rather than treated as a same-day correction.

**Not verified on-device, flagged throughout:** the keyboard-flush fix (item 1), the approximated keyboard-backdrop colour and corner radius (items 2, 10), and the estimated (not rendered) text-width figures behind item 4's audit — none of this session's checks substitute for the on-device confirmation the user has caught real issues with twice already this session (the arrow-fit margin, and now the flush-gap itself, both previously reported as fine here and found not to be).

## 2026-08-11 — Third same-day pass: arrow moved inside the chip, docked-bar colours corrected again, AI-amend moved inside the pill; real Send-button bug found and fixed on Going Quiet

Corrections from further on-device testing of the second pass above (items 15-18), plus a separate bug report and UX correction on Going Quiet's completion area, investigated and fixed in the same turn. `tsc --noEmit` and `vitest run` (27/27) both pass.

**15. Dropdown arrow moved inside the circle, right-aligned.** On-device, the arrow floating beside the chip read as ambiguous about which circle it belonged to. `GroupPicker.tsx`'s `circleUnit` is now a `position: "relative"` wrapper sized to the chip's own rendered footprint (works for both the fixed-diameter circle and the grow-to-fit pill shape, since it doesn't need to know which one a given label produced); the arrow `Pressable` is `position: "absolute"` inside it (`right: 6, top: 0, bottom: 0`), overlapping the chip's own right edge rather than sitting beside it. Still an independent tap target — a small `rgba(0,0,0,0.12)` scrim badge behind the glyph gives it visual separation from the label text it now sits on top of. Not verified on-device whether this scrim reads clearly enough, or whether the arrow ever visually collides with trailing label characters on a tight pill.

**16. Docked-bar colours corrected a second time.** On-device, the first correction's `mixColors(colors.surfaceStrong, colors.border, 0.6)` pill fill and its keyboard-grey-leaning outer blend both still read as uniformly too dark, especially in dark mode, where the two source tokens for the pill (`surfaceStrong` #2f362e, `border` #4a5049) are themselves both dark — leaving no lightness contrast between the pill and its surround. Investigated whether Instagram's actual composer colours could be sampled directly, as asked: this session has no way to open Instagram or read real device pixels; a web search surfaced only unofficial fan-made clone projects with self-chosen hex values, not a verified source, so using them would have swapped one guess for a differently-sourced one while claiming a "real match" that wasn't there — flagged rather than done. Instead, made a reasoned, principled adjustment targeting the two specific complaints: pill fill now `isDark ? mixColors(colors.primary, colors.surfaceStrong, 0.35) : mixColors(colors.surfaceStrong, colors.onPrimary, 0.55)` — light mode leans toward `onPrimary` (white, an existing token) for a paler wash; dark mode leans toward `colors.primary` (the vivid accent green, not the dark tokens used elsewhere) so the pill reads as a clearly lighter accent against a darker surround, matching the relationship Instagram's own dark-mode composer has to its background, which the previous value inverted. Outer blend ratio nudged from 0.3 to 0.25 pill-weight, reinforcing rather than undoing that new contrast. Recomputed WCAG contrast (input text `colors.text` against the new pill fill): light-normal ≈ **12.8:1** (up from 9.55:1); dark-normal ≈ **6.0:1** (down from 9.19:1) — still comfortably past AA's 4.5:1, but a real, reported trade-off: lightening the dark-mode pill for the "too dark" fix costs contrast margin against the near-white dark-mode text. **Still flagged, not resolved:** this remains a reasoned approximation, not a sampled Instagram value — true pixel-matching needs either a real screenshot or the user sampling and reporting an exact hex from their own device (e.g. via iOS's built-in Markup colour picker or macOS's Digital Color Meter), which this session cannot do itself.

**17. AI-amend icon moved inside the pill.** `DockedInputBar.tsx`'s sparkle/sparkles-outline toggle (icon choice unchanged, per direct confirmation — sparkle is the established cross-industry "AI action" convention) moved from a separate `Pressable` outside `styles.pill` to the leftmost element inside it, alongside the mic and send icons — all three now live inside the one continuous pill shape. Caught and fixed in passing, unflagged in the prior pass: `doneButton` had been shrunk to 36×36 for the pill redesign, under the documented 44pt accessibility tap-target floor; added `hitSlop={8}` to it and to the new AI-amend button (28×28 visual box) to bring both back to an effectively ≥44pt hit area without inflating the pill's own visual height.

**18. Confirmed: docked-bar input text is 16pt** — `styles.input.fontSize` in `DockedInputBar.tsx`, unchanged this pass. Matches the suggestion row's own text (also 16pt) and sits inside hold-book's documented 15-17pt Compact-text range; not a smaller size specific to this component.

**Bug + correction: Going Quiet's page-level Send button.** Reported as greyed out/non-functional, suspected leftover from the pre-sequential batch-send architecture. Investigated via a full trace of `circleDrafts`/`selectedGroups` sync in `HoldFlowContext.tsx` and the `canSend`/`draftsToSend` gating in `create/people.tsx`: found no stale-state or wiring bug — the button genuinely was, and had only ever been, the one real thing that dispatched a Send. **The premise was corrected, not silently accepted:** the docked bar's own paper-plane does *not* send a Circle's message — for a `circle:` field, its `onDone` only closes the field (the text was already autosaving live via `onChangeText`); actually dispatching the SMS/share sheet only ever happened inside the page-level button's `send()` loop. The real bug was different from the one suspected, but real: `canSend` required *every* visible Circle's draft to have a non-empty message before the single shared button would enable at all, so one Circle still sitting on its intent-choice step made Send untappable even for a different, fully-written Circle right above it — exactly what "greyed out" would look like on-device. Fixed by giving each Circle's card its **own** `CompactSendButton` (`sendCircle(draft)`, extracted from the old `send()` loop body to act on one draft), matching the pattern already used two sections below for personal notes, and on Reconnect/Taking Time's update/Library's Quick message — this also makes real the per-Circle-independent mental model the rest of the screen already implies. The page-level bulk button and its `canSend`/`draftsToSend` gating are removed entirely. **"Done" moved into that vacated position, but only for the not-yet-sent case:** checked whether this removes the only way to complete Going Quiet without sending anything — it does not, but the check surfaced a second, real, pre-existing gap: `PrimaryButton label="Done"` was previously reachable *only* inside the `hasSentAnything` block, meaning **there was already no way to exit Going Quiet at all if nothing had been sent** — not "leftover from before," a gap in the original per-Circle Send spec itself, which only ever described Done as reachable "once at least one Circle has been sent." Fixed with a second, unconditional `PrimaryButton label="Done"` rendered when `!hasSentAnything`, calling the same `finish()` (already safe to call with nothing sent — `syncAudience`/`recordPostSendChoices` both no-op with no open period). `hold-book/04-ux-content/01-core-journeys.md` point 4 and point 7 updated to match, both marked as corrections to specific same-day-earlier phrasing ("Send fires for whichever cards are currently visible" → per-Circle independent buttons; "reachable as soon as at least one Circle has been sent" → reachable from the very start too), not silently rewritten in place.

`tsc --noEmit` and `vitest run` (27/27) both pass. **Still flagged, not resolved:** no on-device verification possible from this session — the arrow/diameter fit (especially "Close ▼"'s tight margin), the blended-fill colours, and the "zero gap against the keyboard" claim specifically all need a real device check.

## 2026-08-11 — Going Quiet redesigned: sequential group-based sending, combination-keyed templates, provisional Circle-split; Reconnect's Personalise upgraded to match

Full architecture change, done only after an explicit reconciliation pass against the actual current code, per direct instruction not to build fresh on top of a possibly-conflicting structure. `tsc --noEmit` and `vitest run` (27/27) both pass.

**Reconciliation findings, reported before implementing, not silently resolved:**
- The per-Circle cards (`GoingQuietMessageBox`, one `RecipientPersonalisation`/`CompactSendButton` per Circle, from the last three same-day passes) directly conflicted with "exactly one text-entry surface" — confirmed for full removal, not adaptation.
- "The Send/Edit/Personalise + Conversations-dropdown pattern already built for Reconnect" doesn't literally exist in `reconnect.tsx` — its own completion state only ever did a plain `router.push("/library")`. The actual rich mechanic lives in `app/(tabs)/library.tsx`'s `PersonaliseAccordion`. Confirmed: extract it, use it in both places, upgrade Reconnect's own completion step to match rather than leaving it inconsistent.
- `app/create/review.tsx`, named as a file this would touch, doesn't exist anywhere in the repo — confirmed to fold the new completion step inline into `people.tsx` instead.
- No "bundle removed people into a new Circle" mechanism exists anywhere to reuse (grepped repo-wide) — item 8 is new, built on the existing `PENDING_CIRCLE_ID_PREFIX`/`toggleGroup` conventions specifically so the already-fixed `syncAudience()` behaviour (Reconnect's permanence prompt firing regardless of send) covers it for free.

**1. Extracted `PersonaliseAccordion`** from `library.tsx` into `src/components/PersonaliseAccordion.tsx`, unchanged internally — `library.tsx` now imports it instead of defining it locally; its dead local styles (`personaliseBlock`, `accordionPanel`, `input`, `friendMessageSettled*`, `styleChip*`, `accordionActions`) removed. New `src/hooks/usePersonaliseCompletion.ts` owns the per-person open/draft/style/reply-target state and two loaders: `seedAndLoad(candidates)` (seeds via `seedPersonaliseRecipient`, idempotent, then reads back — for Going Quiet's excluded-and-not-yet-personalised recipients, who aren't `ConversationPerson` records yet) and `loadAlreadySeeded(phoneNumbers)` (read-only — for Reconnect, whose `send()` already calls `seedFromAudience` for the whole audience regardless of the Personalise choice, so there's nothing left to seed). New `src/components/PersonaliseCandidateList.tsx` renders the accordion per person from the hook's state.

**2. `templateService.ts` extended with combination-keyed templates**, parallel to the untouched single-Circle scheme: `combinationKey(circleIds)` (sorted, joined), `saveCombinationTemplate`/`getCombinationTemplate` (own SecureStore index/prefix), `getStartingPointsForCombination(circleIds)` (single-Circle defaults of a combination's own member Circles, for the "start from X's message" offer), `deleteAllCombinationTemplates` (wired into `SettingsDrawer.tsx`'s full-wipe alongside the existing `deleteAllTemplates`).

**3. `HoldFlowContext.tsx`/`types/hold.ts`:** removed `circleDrafts`/`GoingQuietCircleDraft` and their setters (`setCircleDraftIntent`, `setCircleDraftMessage`, `applyGeneratedTemplate`, `saveCircleDraftAsDefault`) entirely — confirmed via grep these were used nowhere outside this context and `people.tsx`. `toggleRecipientIncluded` gained a second `fallbackMessage` parameter (the exclude-prefill text, now passed in by the caller rather than looked up from the now-removed `circleDrafts`). New `recipientCircleOverrides: Record<contactId, circleId>` state plus a `splitRecipientsIntoNewCircle(contactIds, group)` method for item 8 — `mergeGoingQuietRecipients` updated to apply overrides *before* its normal per-Circle pass; without this, a split person's real (untouched) Circle membership would keep winning the phone-number dedup and silently reattaching them to their original Circle every time selection recomputes.

**4. `GroupPicker.tsx` simplified** — removed `reselectedCircleIds`/`onToggleReselected`/`expandedCircleIds`/`onToggleExpanded` and the arrow overlay entirely. Both existed to solve problems (an independently-persisted draft per Circle needing a "reselect to resend" distinction; viewing a sent Circle's card without reselecting it) that don't exist any more now every Circle-combination shares one message — `isSelected`/`hasSentThisSession` now map directly onto `AdaptiveCircleChip` the same simple way `app/return/update.tsx`'s own picker already did. "All" no longer skips sent Circles (matching update.tsx — sent Circles aren't locked, item 9).

**5. `app/create/people.tsx` — full rewrite.** One shared `message`/`intent`/`savedDefaultText`/`startingPoints` state for the current `selectedGroups` combination, not per-Circle. `handleToggleGroup`/`submitNewCircleName`/`splitIntoNewCircle` all compute the *next* Circle set locally (since context's own `selectedGroups` hasn't re-rendered yet at the point they need it) and call `loadMessageForSelection(nextGroups, previousText)`: 0 groups → blank; 1 group → existing single-Circle template lookup, unchanged; 2+ with a saved combination default → auto-load it; 2+ with none → offer starting points, carry the previous group's text forward as the field's actual value (starting points are optional taps, not the only path). `send()` sends one shared message to the union of included recipients across the current selection, marks only Circles that actually contributed an included recipient as sent (`recordSendChannel` per contributing Circle, not blanket), auto-saves the combination default silently if 2+ Circles, then clears the selection back to empty via a `toggleGroup` loop per selected Circle — the "repeat" part of the loop. `RecipientPersonalisation` now renders once per currently-selected Circle (Close first, via `orderedSelectedGroups`), all under the one shared message box, not one per card. Item 8's UI: a `bundleSelectedIds` set threaded into `RecipientPersonalisation` (new `bundleSelectedIds`/`onToggleBundleSelected` props, a checkbox on each removed row) plus a screen-level "+ New circle from N selected" link. Completion section: "Not now"/"Personalise" choice (renamed from "Yes") gated on `hasSentAnything`, `usePersonaliseCompletion`'s `seedAndLoad` triggered on "Personalise," `PersonaliseCandidateList` rendered in place of the old inline compose-box-per-recipient; OOO/Done unchanged from the prior pass's fix.

**6. `app/return/reconnect.tsx`:** "Personalise" (`SecondaryButton`) no longer calls `goToConversations`/`router.push("/library")` (removed) — now toggles `showPersonalise` and calls `openPersonalise()` (`usePersonaliseCompletion().loadAlreadySeeded`, phone numbers drawn from `period.audienceCircles`/`audienceUngrouped`), rendering `PersonaliseCandidateList` in place, label switching to "Hide" while open. "Not now" unchanged (`router.replace("/")`, direct exit). `Screen`'s `dockedInput` gained the same `personalise.replyTarget`-first priority `library.tsx`/`people.tsx` already use.

## 2026-08-11 — Queue-based Going Quiet, ad-hoc circle mechanic replacing Personalise; two real "All" bugs found and fixed; Reconnect completion/checkmark fixes

Consolidated redesign from on-device testing of the same-day restructure above, done only after re-reconciling against current code and reporting back (per direct instruction), same as the prior pass. `tsc --noEmit` and `vitest run` (27/27) both pass throughout.

**Investigated first, reported before building:**
- **Dropdown-arrow regression (confirmed):** the prior pass's GroupPicker.tsx rewrite removed the arrow's rendering entirely along with the reselect/expand machinery it used to sit inside — its actual purpose (viewing/editing a Circle's members on demand) was still needed, only the surrounding machinery was obsolete. Restored, single-expand only this time (below).
- **"All" selection bugs — two distinct, unrelated root causes, not one:** (1) a real regression — the prior pass's `toggleAll` rewrite dropped the `nonEmptyGroups` filter the pre-redesign version had, so empty Circles (e.g. "Amin") were back in scope for bulk selection. (2) A genuine race condition, and the actual explanation for the *non-deterministic* part of the report (Amin skipped on one tap, included on the next): `toggleAll` fired N sequential `await onToggle(group)` calls; each went through `people.tsx`'s `handleToggleGroup`, which read `selectedGroups` fresh from its own render closure. Since `toggleGroup` in context no longer does any real async work (its template lookup was moved out in the prior pass), successive calls could resolve before React re-rendered the caller with the previous call's result, making the loop non-deterministic about which Circles it actually toggled. Fixed structurally, not patched: new `HoldFlowContext.setSelectedGroups(groups)` replaces the whole selection in one atomic `setState`, no loop, no staleness window.
- **Reconnect's reported "transition before Personalise" — investigated, premise didn't match code.** `return/transition.tsx` only ever fires from `app/(tabs)/index.tsx`'s `beginReconnect()`, *before* `reconnect.tsx` even mounts — nothing routes there from inside `reconnect.tsx`'s own Personalise/OOO flow. Reported this back rather than reordering something that wasn't actually out of order — but found a real, related asymmetry instead (below).

**1. `AdaptiveCircleChip`/`GroupPicker.tsx` — arrow restored, single-expand, All fixed, front-reorder.** `GroupPicker` gains `expandedCircleId: string | null` + `onToggleExpanded` (owned by `people.tsx`, one value not a set — only one Circle's member list open at a time) and `onSetSelection: (groups) => void` (wired to the new atomic context method). `toggleAll` restores the `nonEmptyGroups` filter and now also **remembers the pre-tap selection and restores it exactly on the deselect tap** (a genuine divergence from `update.tsx`'s own "All" — that one just clears to empty — done because it was explicitly asked for here). New `displayGroups` memo reorders the row so currently-selected Circles float to front, recomputed live on every selection change (item 3) — deliberate, not something to guard against for stability.

**2. `RecipientPersonalisation.tsx` — substantially simplified.** Excluding a recipient's main-list circle now removes them from this component's own rendering entirely (no more inline excluded sub-row with its own instant-message/Personalise toggle) — they move into the screen-level removed-people roster instead (item 3 below). New "+" row pinned first (matches the app's established "+ goes first" convention), bordered circular glyph only — no "Add person" text, per direct correction mid-thread — styled distinctly from name rows so it can't be mistaken for a person while scanning. `bundleSelectedIds`/`onToggleBundleSelected` props from the prior pass are gone — bundling now happens entirely from the new top-level roster, not per-Circle.

**3. `app/create/people.tsx` — full rewrite, three new mechanics.**
- **Queue model:** new `queuedGroupIds: Set<string>`, monotonically growing (a `useEffect` unions in every newly-selected Circle id, never removes one) — used only to gate auto-complete. A second `useEffect` watches `sentCircleIds`/`queuedGroupIds`; once every queued id has been sent to, calls `finish()` automatically (guarded by `autoFinished` so it only fires once). "Done" (`SecondaryButton`, resized from the prior pass's full-width `PrimaryButton`, matching Reconnect's own "Not now"/"Personalise" convention) stays manually tappable throughout as an early exit, same `hasSentAnything` gate as before.
- **Removed-people roster, replacing Going Quiet's Personalise integration entirely** (Reconnect/Library keep it unchanged — only Going Quiet's own usage of `usePersonaliseCompletion`/`PersonaliseCandidateList` was removed from this file). New `RemovedPerson[]` state, growing across every Circle's dropdown this session, each tracked `claimed: boolean` (true once bundled into at least one provisional Circle, never reset). Rendered as a horizontal comma-separated row at the top of the screen; tapping a name toggles it into `bundleSelectedIds`; a "+" bundles the specific selection, or — nobody selected — every currently-unclaimed person. **Flagged, not silently resolved:** re-adding an already-claimed person to a *further* bundle was confirmed as "should not cancel their existing message, they get both" — but the existing `mergeGoingQuietRecipients` dedupes by phone number and `recipientCircleOverrides` is a single-value-per-contact map, so a person can only ever be actively attributed to *one* Circle for send purposes at a time; re-bundling someone in the current implementation moves them to the new Circle rather than genuinely duplicating them into both. Achieving literal "both" would need a real data-model change (multiple simultaneous recipient entries per person) not built in this pass.
- **Post-send state:** `justSentText`/`justSentGroups`/`justSentSaved`, set at the end of `send()` instead of just clearing to blank — shown in place of the empty-selection state until the next Circle is tapped. Single-Circle sends show real "Save to Library" (`saveJustSentSingle`, only if not already matching the saved default); combinations show a static "✓ Saved as Template" (send() already auto-saves the combination default unconditionally, so this is a confirmation of something that already happened, not a separate action — confirmed as the wanted behaviour, correcting the prior pass's silent-only design).
- **Docked bar sends immediately for the group-message field** — `onDone` calls `send()` directly instead of just closing back to a preview, a deliberate, explicit exception to the app-wide "paper-plane only ever means done editing" rule, scoped to this one field. The separate `CompactSendButton` stays too, for sending an unedited auto-loaded default without ever opening the keyboard.
- **"+ Add person" reaches into a Circle's own dropdown** (new `handleAddPerson`, `pickContact()` + `addContactToGroup()` — no such entry point existed anywhere in Going Quiet before this; the closest real precedent was Settings' Manage Circles, whose same underlying service calls this now reuses). A newly-added member needs a new `HoldFlowContext.updateSelectedGroup(updatedGroup)` to actually show up — `toggleGroup`/`setSelectedGroups` only ever use whatever `CircleGroup` object the caller already has, they never refetch from storage. A freshly-created Circle (top-level "+") now auto-opens its own dropdown immediately after creation, so "+ Add person" is right there without an extra tap.
- **Removed:** the `groupLabel` "Message to X, X, X" line, entirely. `setRecipientIndividuallyRemoved`/`setRecipientInstantMessage`/`setRecipientRouteToPersonalise` are no longer called from this screen (the mechanics they drove — inline instant-message, Personalise routing — are superseded by the roster above) — left as still-defined, now-vestigial context methods/type fields rather than deep-cleaned in this same pass; flagged as a real follow-up cleanup opportunity, not silently left without comment.

**4. `DockedInputBar.tsx` — icon positioning corrected.** `row`/`pill` `alignItems` changed from `flex-end` to `flex-start`, plus more generous top/horizontal pill padding — icons now sit at the top of the pill, following its rounded top edge, rather than at the bottom where the outer two were clipping/overlapping the rounded corner.

**5. `app/return/reconnect.tsx` — two fixes.** Checkmark removed from both label-prefixing sites (`✓ ${name}` → plain `name`) — confirmed as the actual cause of the reported pill-inflation: the checkmark was prepended into the measured label text itself, not a separate badge, so `AdaptiveCircleChip`'s circle-fit check could tip a true circle into pill mode for a long-enough label; Reconnect's own chips (Circle names *and* individual people's names, often longer than Going Quiet's short Circle names) were the ones actually hitting that threshold, though the same underlying "✓ in the label" pattern is unchanged and still present in `GroupPicker.tsx`/`update.tsx` — flagged as latent, not touched, since it isn't reported as broken there. Completion now ends at `/return/done` (renamed `notNow` → `finishReconnecting`) instead of straight home — corrects a real asymmetry with Going Quiet's own `finish()` → `create/done.tsx` pattern; `return/done.tsx`'s own "Done" button already handles `endReconnecting()`/`resetFlow` generically regardless of entry path, so no change needed there.

**7. hold-book updated**: `03-mvp-requirements.md`'s Going Quiet and Reconnect P0 lists rewritten for the new model; `04-ux-content/01-core-journeys.md`'s Going Quiet numbered list (points 1-7) and Reconnect's point 5 fully rewritten, not patched; new "Personalise pattern" section in `04-ux-content/04-navigation-architecture.md` documenting the shared mechanic across all three screens and what still stays a plain nav (Library's own reply flows, `return/done.tsx`'s "Finish Reconnecting").

## 2026-08-11 — Individual-by-default delivery with a per-Circle group toggle; Reconnect brought to parity with Going Quiet's queue/reorder/Done model; a real audience-data-corruption bug found and fixed

Consolidated 17-section pass (numbered 0-16 in the originating instruction), worked in the stated priority order — section 0 first, since it corrected a wrong premise a previous instruction had built on. `tsc --noEmit` and `vitest run` (27/27) both pass throughout.

**0. Delivery model corrected: individual/BCC-style by default, not one shared group message.** A prior instruction to make combination sends dispatch as one shared group message was wrong and is superseded here, not built on further. The person still composes one shared draft and takes one Send action for a selected Circle-combination — that part stands — but by default each recipient now gets their own separate message, with no visibility into who else received it or that it was sent to anyone else at all, since people sharing a Circle may not know each other. `CircleGroup`/`AudienceCircle` both gained `sendAsGroup?: boolean` (default/undefined = individual); a new `Switch` toggle, default off, is available both when creating a Circle (`GroupPicker.tsx`'s naming-active panel, `app/settings/circle/index.tsx`'s own creation panel) and from an existing Circle's own settings in Manage Circles (`circleService.setSendAsGroup`). `smsService.ts` gained `sendToCircles(targets, message)`: for a `sendAsGroup` target, one `sendOrShare(numbers, message)` call (one compose thread, all recipients see each other, unchanged expo-sms behaviour); for an individual target, one `sendOrShare([number], message)` call per recipient, sequential, try/catch per recipient so one dismissed/failed compose sheet doesn't block the rest. **Confirmed technical consequence, not silently absorbed:** `expo-sms`'s `SMS.sendSMSAsync` has no OS-level way to dispatch messages silently — each individual recipient's send requires its own native compose UI and a manual tap to actually send, so an individual-delivery Circle of N people means N separate compose-sheet taps per Send, not one. **Mixed-setting combinations, confirmed (not asked about first, built directly):** a Send across Circles with different `sendAsGroup` settings has each Circle follow its own setting independently within that one Send action — `people.tsx`/`reconnect.tsx`'s `send()` both build one `CircleDeliveryTarget[]` per Send (one entry per Circle, carrying that Circle's own `sendAsGroup` and its included recipients) and pass the whole batch to `sendToCircles` in one call. `HoldFlowContext.buildAudienceCircles` carries `sendAsGroup` through into the persisted `AudienceCircle` so Reconnect (which sends independently, later, from `period.audienceCircles`) honours the same per-Circle setting the person chose at Going Quiet time. Sent-state tracking (`recordSendChannel` per Circle) is unaffected either way — it already only cared about *whether* a Circle was reached this session, not how.

**1 & 4. Investigated, not built — neither bug exists in current code.** Reported: Reconnect shows a text-labelled "New Circle" after "All" instead of a "+"-only button positioned first, matching Going Quiet; and selecting "All" also selects "+". Grepped and read `reconnect.tsx` in full: it has no "+"/"New Circle" affordance anywhere in its picker row at all — Circle creation isn't a Reconnect capability, only Going Quiet's — and `coverage.totalIds` (what "All" selects) is built from `getReconnectCoverage`, which only ever includes real audience Circles/contacts, never a "+"-type entry. Flagged back rather than inventing a fix for behaviour that isn't there.

**2. Going Quiet's Save/Change-template UI confirmed present and working** (`people.tsx`'s post-send `messageControls` block — "Save to Library"/"✓ Saved to Library" for a single Circle, "✓ Saved as Template" for a combination), no further change needed. **Reconnect has no equivalent at all**, and building one isn't a small addition — Reconnect's message starts from a fixed `QUICK_RECONNECT_MESSAGES` list, not a per-Circle saved default, so "save"/"change template" would need a real design decision about what it saves against. Logged as an open question in `hold-book/08-decisions/04-open-questions.md` rather than guessed at.

**3. Removed Reconnect's "X of Y reached" header line entirely**, not replaced with different phrasing — `app/return/reconnect.tsx`'s `progressText`/`chipRow` `<Text>` and its now-unused style both deleted. **Found a related, still-live instance while checking the app's own established no-counts rule**, not part of what was reported: Home's "Continue reconnecting" widget (`app/(tabs)/index.tsx:417`) still shows the identical `${done} of ${total} reached` count. Left untouched — it wasn't the reported bug, and changing it without being asked risked overstepping — logged instead in `hold-book/08-decisions/04-open-questions.md`.

**5. `DockedInputBar.tsx` corner radius reduced**, `theme.radius.lg` (24pt) → `theme.radius.sm` (10pt) on the bar's top corners, plus wider `pill` horizontal padding for clearance — 24pt read as too aggressively curved on-device and visually clipped against the AI-amend/send icons near the bar's own top corners; 10pt is a subtler curve closer to the keyboard's own corner style. Applies everywhere this one shared bar renders, not just Going Quiet/Reconnect.

**6. Send fires immediately, everywhere, confirmed.** Going Quiet's own docked-bar `onDone` for the `group-message` field was re-checked against the current code (not assumed carried over from the prior pass) and confirmed still correct: `void send(); setActiveField(null);`, no intermediate "back to preview" step. Reconnect's own docked-bar `onDone` had the bug this was meant to fix everywhere — it only closed the field, relying on the (individual, per-recipient, no fan-out) old `sendOrShare` path having been wired to a different button entirely. Fixed to match: `void send(); setMessageFieldActive(false);`.

**7. Reconnect gained a "Done" button**, `SecondaryButton`, beside `CompactSendButton` in the `!coverage.complete` branch's `sendRow`, gated on `coverage.contactedIds.length > 0` (Reconnect's equivalent of Going Quiet's `hasSentAnything`), calling the existing `finishReconnecting()`. Matches Going Quiet's model exactly: manual early-exit only, not shown until something's actually been sent, not needed once every Circle/contact has already been reached (that path completes on its own via the `coverage.complete` render branch-switch, no separate auto-complete effect required on this side — see item 12).

**8. Confirmed already correct, no code change needed.** `AdaptiveCircleChip`'s existing priority — `isSelected` drives the ring only (`chipSelected`/`chipSelectedOutline`), independent of fill; `showSentFill = !isSelected && hasSentThisSession` drives the desaturated sent fill, mutually exclusive with the ring — already matches "currently selected, not yet sent uses the ring, not the dark-green-fill/white-text treatment." The greyed-out-once-reordered part of this item was the real gap, closed by item 9.

**9. Reorder + grey-out now both key off entering compose, not each selection tap, in both flows.** Going Quiet's half (`GroupPicker.tsx`) already had this from an earlier same-day pass (`composingActiveIds`, frozen/released via a render-time conditional `setState` — React's own documented "adjust state during render" pattern — triggered by `isComposing`); this pass added the matching grey-out (`circleUnitGreyed: { opacity: 0.4 }` on any chip not in the frozen set) alongside the reorder that already existed. Reconnect's half was built fresh this pass, same technique: new `composingActiveIds` state and freeze/release block keyed on `messageFieldActive`, a generic `orderPills<T>(items, idOf)` helper applied to both `circlePills` and `ungroupedPills`, and a wrapping `View style={isGreyedOut && styles.chipGreyed}` around each chip using the same 0.4-opacity treatment.

**10 & 11. Confirmed already fixed, from an earlier segment of this same pass** — `GroupPicker.tsx`'s `arrowButton` no longer uses `top:0, bottom:0` (full-chip-height, vertically-centred — the same position as the centred label text, which was the actual overlap cause); now `bottom:8`, anchored near the chip's bottom edge. Reposition was chosen over growing the circle again, since the diameter had already grown four times the same day for unrelated reasons. `people.tsx`'s removed-people roster section now renders after `<GroupPicker>` in JSX order, not before — the circle row stays topmost, roster second, text box third.

**12. Real bug found and fixed: `finish()`'s `syncAudience` call was silently truncating/erasing `period.audienceCircles`.** Investigated against a specific repro (2 Circles selected as the full queue, message edited, Send tapped — auto-complete expected, didn't fire, screen showed the plain selection state instead). `finish()` called `syncAudience({ audienceCircles: buildAudienceCircles(selectedGroups) })` — but `selectedGroups` is cleared back to empty by `send()` immediately after every send, so by the time `finish()` ran (whether via the manual "Done" tap while nothing's selected, or via the auto-complete effect, which by construction only fires once `send()` has already cleared the selection), `buildAudienceCircles([])` overwrote `period.audienceCircles` with an empty (or, for a manual Done tap made while a *different*, smaller selection was active, an incomplete) set — silently dropping every previously-sent Circle from the record `syncAudience`'s own doc comment says it exists to protect. This corrupts the exact data Reconnect's `getReconnectCoverage` depends on entirely (`period.audienceCircles`/`audienceUngrouped`), so a truncated/emptied set there would explain downstream Reconnect coverage problems too, not just a Going Quiet symptom. **Fixed** with a new `queuedGroups: Map<string, CircleGroup>` state, populated by the same effect that already grows `queuedGroupIds` (every Circle ever selected this session, monotonic, never shrinks) — `finish()` now calls `buildAudienceCircles(Array.from(queuedGroups.values()))` instead of `buildAudienceCircles(selectedGroups)`. **Reconnect's own completion mechanism investigated separately and confirmed to have no analogous bug** — `getReconnectCoverage` is a pure function recomputed fresh every render straight off `period` state (no memoisation, no effect-timing window to go stale in), so once this fix stops `period.audienceCircles` itself from being corrupted, Reconnect's coverage/completion switch should already be correct without any changes on its own side. **Not fully verifiable from this environment:** no device/simulator access here, so the exact "returned to the selection screen" symptom couldn't be reproduced and re-tested end-to-end — the `syncAudience` bug is real, confirmed by code reading, and directly explains an emptied/truncated `audienceCircles`, which is either the whole cause or a compounding one; flagged rather than claimed as a certain, fully-verified fix.

**13. Your Circles' main Circle row position — investigated, no code-level bug found.** `app/settings/circle/index.tsx`'s JSX has the Core section immediately followed by the main `pillRow` ScrollView, with no intervening element and only the screen's own uniform `content: { gap: theme.spacing.lg }` between every sibling — no extra margin/padding found. The Core section's own copy paragraph, "Where this comes from" link, and its own `corePillRow` are real, sizeable content that pushes the main row down by design, not by a spacing bug — flagged as a content-length/design question, not fixed speculatively.

**14. Scroll broken on Our Mission/Research/Hold+ — investigated three ways, no code-level bug found.** `Screen.tsx`'s own existing comment claims this exact class of bug (input-light screens not scrolling, due to `KeyboardAvoidingView`'s `onLayout` never firing without a keyboard event) was already root-caused and fixed by switching to ScrollView-only with `automaticallyAdjustKeyboardInsets`/`contentInsetAdjustmentBehavior="automatic"` — current code already reflects that fix. All three screens are structurally identical (plain `<Screen>` wrapping `<Text>`/`<View>` children, no `dockedInput`, no `footer`) to screens confirmed scrolling fine. Navigation registration (`app/_layout.tsx`) has nothing distinguishing these three `Stack.Screen` entries either. Flagged as unresolved — needs live device inspection, not claimed as fixed without one.

**15. Oversized "Done" reappearing after nav-back from Your Circles — repositioned, root cause not conclusively identified.** `doneButton` moved out of `messageControls` into the same `sendRow` as `CompactSendButton`, in both the composing and just-sent render branches — matches the already-confirmed `SecondaryButton` sizing exactly, and there is only ever one `doneButton` definition reused in both places, no duplicate render path in the current code that could produce two. Most likely explanation, given this project's standing pattern: a stale Fast Refresh bundle after a structural navigation change, not a genuine logic bug — flagged as such rather than guessed at further.

**16. OOO/status-before-Transition sequencing — confirmed already correct on both flows, no code change needed.** Going Quiet: the "OOO and status" section (collapsed by default, `oooExpanded` initial state `false`) renders on the same screen as Done, gated by the same `hasSentAnything` condition Done itself uses — it's visible and addressable (open to review, or left explicitly collapsed) before Done is ever tapped, so tapping Done doesn't skip past an unseen step; `finish()` (Done, or auto-complete) always runs after this point, never before. Reconnect: the equivalent "OOO and status" section, also collapsed by default, sits on the same "Everyone's been reached" screen above `finishReconnecting()`'s own "Not now" button — same structure. The open question about default expand state is resolved by what's already built: collapsed by default, matching the "reveal on demand" pattern point 2/section 2's own dropdown arrow uses — this matches the recommendation given, not a silent decision, and is called out here for confirmation rather than assumed settled.

**hold-book updated**: `04-ux-content/01-core-journeys.md`'s Going Quiet points 1, 5, 6, 7 and Reconnect points 1, 3, 4 corrected in place (reorder/grey-out timing, delivery-model default, roster position, Done position, no-counts line, auto-complete/Done parity); `04-ux-content/04-navigation-architecture.md`'s dropdown-arrow and docked-bar-corner-radius sections corrected with this pass's further fixes; two new entries in `08-decisions/04-open-questions.md` (Reconnect's missing save/template mechanism; the Home-widget count still contradicting the no-counts rule this pass enforced on Reconnect's own screen).

## 2026-08-12 — Reconnect's completion folded into one screen; a real sent-fill regression fixed; Home's remaining count removed; Conversations rebuilt around per-person reply pills

`tsc --noEmit` and `vitest run` (27/27) both pass throughout.

**1. `app/return/reconnect.tsx` — no more page-swap once coverage is complete.** The `!coverage.complete`/`coverage.complete` branches used to return two structurally different `<Screen>` trees (different header, circle row present vs. absent, different bottom actions) — no route change was involved, but swapping the entire visible screen read on-device as navigating to a separate page. Merged into one continuous render, conditional in place: the circle/contact row now renders unconditionally (stays visible once fully sent, matching "the person should still see who they messaged"); the docked message box only renders pre-complete; once complete, the space it vacated shows an inline "Want to reply to anyone properly?" Personalise/Not-now choice instead. "Not now" no longer finishes Reconnect — since there's no longer a separate completion page to land on, it just collapses that choice (reveal-on-demand, dropdown-arrow-style, reopenable) rather than dismissing it. **This left a real gap to resolve, flagged rather than silently invented:** with "Not now" no longer the finishing action, something else had to be — reused the existing "Done" button (`SecondaryButton`, already built for early-exit) unconditionally whenever `coverage.contactedIds.length > 0`, regardless of `coverage.complete`, so it now doubles as the actual way to leave once everyone's been reached, not just an early exit. `finishReconnecting()` itself unchanged (`router.replace("/return/done")`).

**2. OOO/status default-expand reversed, scoped to this one point in the flow only.** `oooExpanded`'s initial state flipped `false` → `true`, a deliberate, confirmed correction (not an open question) to item 16 in the entry above ("OOO/status-before-Transition sequencing... collapsed by default... matches the recommendation given") — that recommendation is now explicitly reversed for Reconnect's own post-coverage-complete moment specifically. Going Quiet's own OOO section (`app/create/people.tsx`) was **not** touched — whether it should match is logged as its own open question, not assumed.

**3. Real regression found and fixed: `AdaptiveCircleChip`'s sent-fill was never actually dark green.** Reported as "circles not showing the sent-state fill in Reconnect, previously confirmed working." Investigation found `chipSent`'s `backgroundColor` was `colors.surfaceStrong` — the exact same value as the default *unselected* chip (`chipSecondary`) — with only a slightly muted text colour distinguishing it, nothing close to the documented "dark-green fill, white text" standard (see this log's 2026-08-11 "Close Circle-colour reversal" entry, which explicitly describes every Circle using "the exact same sage-default/dark-green-sent-fill treatment"). Since `AdaptiveCircleChip` is the one shared component Going Quiet, Reconnect, and Taking Time's "Send an update" all render sent chips through, this was never actually Reconnect-specific — all three surfaces were equally affected, not just the one reported. Fixed at the shared-component level: `chipSent` → `colors.primary`, `labelTextSent` → `colors.onPrimary`.

**4. Home's own progress count removed — actually fixed this time, not just logged.** `app/(tabs)/index.tsx`: both the "Continue reconnecting" (Reconnecting state) and "Finish Reconnecting" (Post-Reconnect state) subtexts showed an "X of Y" count, contradicting the app's own no-badges-no-counts rule — the former had already been logged as a flagged, unfixed gap in `hold-book/08-decisions/04-open-questions.md` (2026-08-11) but deliberately left untouched since it wasn't what was reported at the time; both are now unquantified ("Pick up where you left off" / "Continue where you left off"). `postReconnectProgress`/`reconnectingProgress` state and the `PostReconnectProgress` interface removed entirely as dead code — their only consumer was the now-removed count text; the underlying `getReconnectCoverage`/`getConversationProgress` calls that also drive `resolvedState` branching are untouched.

**5. "Close Circle" naming regression fixed in `SafeguardingBanner.tsx`; app-wide audit found no other user-facing instance.** Three user-visible strings ("Notify your Close Circle?", "Sent to your Close Circle.", "Notify someone from your Close Circle now") still carried the pre-2026-08-10 name — the rename to "Close" (`circleService.ts`'s `CLOSE_CIRCLE_NAME`) never reached this component, since these are hand-written Alert/banner copy, not driven by the stored Circle name. Fixed to "Close" throughout, dropping the redundant "Circle" suffix rather than lowercasing it, matching the established rename exactly (see this log's 2026-08-10 entry: "no call site ever appended a 'Circle' suffix" was already confirmed true for every other call site — this one had simply been missed). Grepped the whole app case-insensitively for "close circle": every remaining match is a code comment or an internal migration-constant name (`circleService.ts`'s `LEGACY_CLOSE_CIRCLE_NAME`, `PrimaryButton.tsx`'s comment) — none user-facing, left as-is.

**6. `app/(tabs)/library.tsx` — Conversations rebuilt around per-person reply pills, replacing the older per-circle-card pattern.** Investigated first, as instructed, before rebuilding: this screen's existing mechanic (one expandable card per Circle, an embedded shared `DockedFieldPreview`, a "Show/Hide people" member list with per-member include/exclude checkboxes, and a parallel "quick message vs. Personalise" swap for excluded members) was confirmed to predate and diverge from the shared `PersonaliseAccordion`/`usePersonaliseCompletion` work — it re-implemented its own local `personaliseSwapIds`/`personaliseDrafts`/`personaliseStyles`/`personaliseReplyTarget` state rather than reusing the hook, and had no "+", no bold "+"/"All", and no dropdown-arrow/pill pattern at all. Rebuilt: each Circle chip in the top row gets the same independent dropdown-arrow tap target `GroupPicker.tsx` uses (visually identical `circleUnit`/`arrowButton`/`arrowBadge` treatment). **Pill row, corrected mid-pass from an initial per-circle-sub-row draft to one shared row:** there is exactly one pill row, positioned directly beneath the main circle row, not a separate pop-up under each individual circle. A circle's arrow adds only that circle's own people into the shared row; several circles can be expanded at once, with people ordered to match their parent circle's own left-to-right position above (`expandedPeople`, a `useMemo` that filters+flatMaps `circleSections` in its existing order — already correct without a separate sort, since `circleSections` itself is built in the same order the chips above render in). Collapsing a circle removes its people from the row again; if the person whose accordion was open belonged to that circle, `toggleCircleExpanded` closes the accordion too (nothing floats with no matching pill above it) — their draft is untouched, since it lives in `personaliseDrafts`/`replyStorageService`, never cleared by this. Pills use the same standard `AdaptiveCircleChip` sizing as everywhere else (no override). Tapping a person's pill opens their `PersonaliseAccordion` inline, reusing the exact same component Going Quiet/Reconnect's completion steps already use rather than inventing a new compose surface. Sent-state (dark-green fill) is now driven by `person.completed`, shown **per person**, not per Circle — a deliberate difference from Going Quiet/Reconnect, per the given framing that Conversations is a per-person reply checklist, not a broadcast send — and, confirmed explicitly, never locked: a sent pill's `onPress` is unguarded, so tapping it again reopens the same accordion to send a further message, matching the "sent Circles are never locked" rule used everywhere else this chip pattern exists. "+", positioned first in the row, now maps to the existing `addNewPerson()` contact-picker flow (adds an ungrouped person) — the old full-width "+ Add person" button removed as redundant. "+" and "All" both render bold (`labelBold`).
   - **A real, necessary capability removal, not an incidental one:** the old per-circle "send one shared quick message to everyone in the Circle at once" action (`sendCircle`, with its own included/excluded-member bookkeeping) is gone — there is no equivalent in the new per-person model, and none was described in what was asked for. This follows directly from the given framing ("not a broadcast-style group send"), but is flagged explicitly since it is a genuine feature removal, not a restyling.
   - **A capability quietly dropped, flagged rather than silently absorbed:** the old per-member "mark complete without sending" checkbox (for a reply handled outside the app) had no natural home in the new pill/accordion layout and was not reinstated — still preserved, unchanged, for ungrouped people's own cards, which this rebuild left untouched.
   - **`allIds`/`toggleAll`/"All" narrowed in scope, not removed:** with Circle selection replaced by expand/collapse, `selectedIds`/"All" now only ever govern ungrouped people (whether their own card is open) — unchanged in behaviour from before, just no longer spanning Circles, since Circles never used `selectedIds` for anything else in the new design.
   - **Ungrouped people's own cards (quick message / Personalise-swap / "select 2+ to bundle into a new Circle") were deliberately left untouched** — the given target design only described Circles-with-members; extending the same rebuild to ungrouped individuals, who have no "Circle" to expand into, wasn't asked for and would have meant inventing behaviour rather than following what was specified.
   - **Fixed a bug introduced mid-edit, caught before it shipped:** an early draft of this rewrite derived Templates' Circle-name lookup from the same-session `people` list (`groupByCircle`) instead of `circleService.getGroups()` — which would have silently hidden a saved template's name for any Circle with no current Conversations people in it (e.g. everyone in it already fully replied to and cleared). Caught via `tsc`/manual review before commit; restored the original `getGroups()`-based lookup.
   - **Flagged, not built: a claimed "three segmented tabs (Conversations/Templates/Research), Conversations default" structure for this screen — no record of this design exists anywhere.** `hold-book/04-ux-content/04-navigation-architecture.md`'s own "Library screen structure" section documents exactly two things: Conversations (primary, top) and Templates (secondary section, below) — a single scrolling screen, not segmented tabs — and says nothing about Research being part of Library at all; Research is a wholly separate destination (`app/settings/research.tsx`, reached via Settings). The current code (both before and after this rebuild) already matches the two-part documented structure. Building a 3-tab structure, or moving Research into Library, would directly contradict this on-record design — not built; flagged for confirmation instead.

**hold-book updated**: `08-decisions/01-decision-log.md` gets a new summary row for this pass, referencing the OOO-default-expand reversal by description (item 16 above) per its own request; `08-decisions/04-open-questions.md`'s Reconnect-widget-count entry marked resolved (the count is gone) rather than left as still-open; a new open question logged for the claimed Library tab structure, since no such design exists on record to build against.

## 2026-08-13 — Real audience-data duplication bug fixed; Reconnect's bundled-Circle naming redesigned; "+"/"All" pinning made an app-wide rule; a second Reconnect Send mechanism removed; Reconnect gets real per-Circle templates; composition-state now drives both swipe-back and the tab bar

`tsc --noEmit` and `vitest run` (27/27) both pass throughout.

**1. Real bug found and fixed: `buildAudienceCircles` let a bundled contact appear in two Circles' audience at once.** On-device: selecting a Circle ("Amin") in Reconnect also auto-selected an unrelated one ("New Circle"), alongside a React "duplicate key" console error keyed by a phone number. Traced to `buildAudienceCircles` (`HoldFlowContext.tsx`): a contact split into a new provisional Circle via `splitRecipientsIntoNewCircle` is never actually removed from their *original* Circle's own `contacts` array — that array is deliberately never mutated, `recipientCircleOverrides` was always meant to be authoritative for reassignment instead — but this function, which builds the *persisted* audience Reconnect reads, never applied that override, so the persisted record ended up listing the same person under both Circles. Since Reconnect's `includedPersonIds` is one flat set keyed by phone number, not scoped per Circle, toggling the shared person via their original Circle's chip also flipped the new Circle's own "everyone included" read — the actual mechanism behind the reported "selecting Amin also selects New Circle." Fixed at the source: `buildAudienceCircles` now takes `overrides` and excludes a contact from any Circle that isn't the one they were actually reassigned to (both `people.tsx` call sites updated). **Also fixed defensively, per direct instruction:** the pill row's React key is now `${circleId}:${phoneNumber}` (`ungrouped:${phoneNumber}` for ungrouped), kept deliberately separate from the `id` used for inclusion/lock/sent-state logic (still the bare phone number, since two entries for the same number really are one recipient) — so a future data-layer duplicate can never again cause React to conflate two list items.

**2. `confirmPendingCircle`'s multi-person bug fixed, then the whole permanence step redesigned.** The immediate bug: it only ever added `circle.contacts[0]` to the real Circle, silently dropping every other bundled person — fixed to add all of them. Separately, the bigger redesign it was step one of: the old yes/no "Add [name] to [Circle] permanently?" gate is gone entirely. A bundled Circle is now made real **immediately and unconditionally** the moment Reconnect loads (inside `refresh()`, via `createGroup(name, false, true)` — new third `needsNaming` param on `CircleGroup`/`createGroup`) — there's no "should this exist" decision left to make, matching the given framing exactly: it already exists, fully formed, from the moment it's created. Its name is no longer a generic "New Circle"/"New Circle N" — `bundleIntoNewCircle` (`people.tsx`) now generates initials ("P & A" for two people, "P, A & J" for three+, via a new `initialsPlaceholderName` helper). The only remaining Reconnect-side step is an **optional rename opportunity**: sequenced after the message box (i.e. once nothing's currently selected to send) and before the Personalise choice, appearing regardless of whether the bundled Circle was actually messaged this session (checked directly against `needsNaming`, not against send/coverage state, so an unmessaged Circle is never silently skipped) — each row shows the placeholder name next to `HoldMark` (the app's own circle glyph, not a generic star, per direct instruction) with "Rename" (opens the shared docked bar) or "Leave as is" (calls the same `renameGroup`, unchanged name, just clears `needsNaming` — a final acknowledgement, not a deferral, so it's never asked again). New `renameGroup` in `circleService.ts` is also Manage Circles' first-ever rename capability (it had none before this — `group.name` was plain, non-editable `<Text>`), reachable independent of this Reconnect step. New `resolvePendingCircleInPeriod`/`renameCircleInPeriod` in `holdHistoryService.ts` keep the period's own `audienceCircles` snapshot (id swapped from the temp `pending-` one to the real Circle, name kept in sync on rename) consistent without needing a fresh visit.

**3. "+"/"All" pinning made an explicit app-wide rule, not just a Reconnect fix.** Confirmed: "+" pinned *outside* the scrollable row (never scrolls away), "All" first *inside* it — `GroupPicker.tsx`'s own existing `pinnedRow`/`newCircleStack` treatment is the reference implementation, unchanged. Applied to **both** of Reconnect's rows (the Circle-browsing row and the shared pill row each get their own "+"/"All" pair — "+" adds an ungrouped person in both places, "All" scoped differently per row: the Circle row's toggles the *entire* audience, the pill row's toggles only what's currently *visible* — a deliberate, non-redundant split, not an oversight) and to `library.tsx`'s own circle-browsing row, which had the same "+"-inside-the-scroll placement bug. **Two places deliberately left unchanged, flagged rather than silently altered:** `library.tsx`'s second (per-person pill) row has no "+"/"All" concept to reposition — it never had one, and inventing one wasn't asked for; `settings/circle/index.tsx`'s own "+" already lives in the header bar, not the row, a *different*, already-decided convention (`08-decisions/01-decision-log.md`, 2026-07: "the heading-icon pattern moved to Your Circles' header instead — it has its own title/header bar Going Quiet's inline step doesn't") — applying the row-pinned pattern there would directly contradict that.

**4. Reconnect's separate always-available Send button removed.** Confirmed, reversing the rationale I'd flagged it under previously (sending an unedited default without opening the keyboard): "Done" is now exit-only, never a send trigger: the docked bar's own send icon is the sole way to send on this screen. **Flagged, not silently extended:** Going Quiet's screen-level `CompactSendButton` sits in the exact same position for the exact same reason and was not touched — left as an open question whether it should match for app-wide consistency.

**5. Reconnect gets real per-Circle/per-combination saved templates, replacing `QUICK_RECONNECT_MESSAGES`.** New `reconnectTemplateService.ts` — deliberately a separate storage namespace from `templateService.ts`'s Going Quiet templates (same mechanics: single-Circle key, sorted-combination key via the shared `combinationKey()` helper; different storage, since a Circle plausibly wants a different saved default for "going quiet" than for "reconnecting" — sharing one key would let one silently overwrite the other). Reconnect's own "contributing Circles" (whichever currently have ≥1 included person, derived fresh from `includedPersonIds`, Reconnect's own equivalent of Going Quiet's `selectedGroups`) drives which template loads/saves, reloading whenever that set's signature changes — same trigger shape as Going Quiet's `loadMessageForSelection`. New built-in fallback when nothing's saved: *"I'm getting there, will send a proper response soon."* — short and instant, deliberately not a fully redrafted reply (that's Personalise's job), per the given framing. "Change template"/"Save" controls added under the message box, matching Going Quiet's own pattern. `QUICK_RECONNECT_MESSAGES` itself, and Library's own separate use of it, are untouched. **Simplified, flagged rather than silently built in full:** "Change template" doesn't reopen a starting-points-style picker (Going Quiet's own multi-source browsing UI) — Reconnect has no equivalent chip/intent-picker concept to hang that on; it resets to the built-in fallback and reopens the field for a fresh edit instead, a deliberately narrower implementation of "change template" than Going Quiet's own.

**6. New nav rule: the bottom tab bar hides during active text composition, anywhere, shows everywhere else — driven by composition state, not a hand-maintained per-screen list.** **Investigated first, as instructed:** no existing dynamic "disable swipe-back while composing" mechanism was found anywhere in the app or either decision log — the only related code is a *static*, unconditional `gestureEnabled: false` on four unrelated completion/transition screens (`welcome`, `create/done`, `return/transition`, `return/done`), nothing tied to composition state. Flagging this rather than assuming an "already fixed" baseline that isn't actually there. **Built as one shared mechanism, per direct instruction, not two:** new `useComposingGestureLock(isComposing)` hook (`src/hooks/`) calls `navigation.setOptions({ gestureEnabled: !isComposing, hideTabBar: isComposing })` — each option only matters on the kind of screen it applies to (`gestureEnabled` on a pushed stack screen, `hideTabBar` on a tab root) and is a harmless no-op everywhere else, so the same one hook works unmodified on Going Quiet, Reconnect, and Library. `BottomTabBar.tsx` checks the *focused* route's own descriptor for `hideTabBar` before rendering, so switching tabs while composing never leaves the bar hidden for a screen that wasn't actually composing. **Investigated the screen list before wiring anything, since the app's actual navigation structure changes what "hide during composition" even means per screen:** Going Quiet and Reconnect are stack screens *outside* the `(tabs)` group entirely — the tab bar was never rendered there in the first place, so "hidden throughout" was already unconditionally true; only `gestureEnabled` (swipe-back) is meaningful for them. Library is the *only* tab-root screen with any composition surface at all (Templates is a section of the same screen, not a separate route, so it's covered by the same `activeField`/`personaliseReplyTarget` check already driving its own docked bar) — Home and History have no composition concept, so neither needed wiring. **Flagged, not silently reconciled:** the request's "Library → Research" framing describes Research as part of Library; it isn't and never has been in this app's record — Research is `app/settings/research.tsx`, reached via Settings, outside the tabs group (so, like Going Quiet/Reconnect, it structurally has no tab bar to hide in the first place) — same premise mismatch already flagged once this session for the claimed Library tab structure.

**hold-book updated**: `04-ux-content/01-core-journeys.md`'s Reconnect section corrected for the removed permanence gate/new naming-opportunity step, the removed Send button, and the new per-Circle template architecture; `04-ux-content/04-navigation-architecture.md` gets a new section documenting the composition-driven tab-bar/swipe-back rule and the "+"/"All" pinning convention as explicitly app-wide; `08-decisions/04-open-questions.md` gains one new open question (Going Quiet's own Send button, for app-wide consistency) and a correction to the "Library tab structure" entry noting the same Research-placement premise recurred.

## 2026-08-13 — Second real audience-persistence bug: exclusion (not just reassignment) wasn't reaching the persisted record either

`tsc --noEmit` and `vitest run` (27/27) both pass.

**The bug, confirmed directly against code, not assumed:** `buildAudienceCircles` (`HoldFlowContext.tsx`) maps from each `CircleGroup.contacts` — the live, permanent Circle — every time it runs. The earlier fix this same day (see the "real audience-data duplication bug" entry above) taught it to exclude a contact *reassigned* to a different Circle via `recipientCircleOverrides`, but that was only half the picture: a contact excluded at Going Quiet (`toggleRecipientIncluded` → `included: false`) *without* ever being reassigned anywhere stayed fully present in the persisted `audienceCircles` regardless — only the one SMS they didn't get was affected, not what Reconnect would later see. This directly contradicts this log's own original, on-record intent (see the "Persisted audience, not re-picked" entry: *"frozen onto the `HoldPeriod` record at send time"* — an older entry, field names since evolved to `audienceCircles`/`audienceUngrouped`, but the stated intent is real and unchanged) — the audience was never actually frozen for this case, it was live-reading the permanent Circle every time `buildAudienceCircles` ran.

**Fixed:** `buildAudienceCircles` gains a third parameter, `excludedContactIds` (a `Set`) — a contact is now dropped from a Circle's persisted `contacts` if they're either reassigned elsewhere (existing check, unchanged) *or* currently excluded (`included: false`) and never reassigned (new check). `people.tsx` computes this fresh from `goingQuietRecipients` at both call sites (the first-send `startHoldPeriod` call, and `finish()`'s `syncAudience` call). Confirmed the double-counting bug from the earlier entry today resolves as a natural consequence of this fix too, not just the reassignment case — an excluded-and-never-reassigned person now has no path into the persisted record under any Circle, so they can't be double-counted regardless of what happens to them afterward.

**Scope, stated explicitly since it matters for how "frozen" this actually is:** `goingQuietRecipients`' `included` flag is current-state, not per-send history — there's no record of "were they included in send #1 specifically, then excluded before send #2." The achievable, implemented semantics are "frozen at `finish()` time, reflecting whatever `included` state is current then" — not a literal per-send freeze. This matches what's practically achievable without a much larger history-tracking change, and matches the original entry's own framing ("frozen... at send time," read here as "the moment the audience is committed," not "a separate snapshot per individual send").

**Confirmed, not changed:** exclusion is scoped to *this Hold period only* — the permanent `CircleGroup.contacts` (the real Circle, in Manage Circles) is never touched by any of this. Someone excluded this round is simply absent from *this period's* persisted audience; they're back to normal, included-by-default status the next time the Circle is used, since the exclusion lived only in session-scoped `goingQuietRecipients` state, which doesn't persist across sessions at all.

## 2026-08-13 — Bottom tab bar: flat colour box replaced with a theme-aware frosted pill; a real dead-end navigation bug fixed in Post-Reconnect

`tsc --noEmit` and `vitest run` (27/27) both pass.

**Item 1/2 — `BottomTabBar.tsx` had a real, findable styling bug: the wrapper painted a flat, hardcoded-to-"normal" solid box (`colors.background`) behind the pill on every screen, regardless of what was actually rendered behind it.** Two things wrong with this, not one: it was a separate opaque container at all (the pill was meant to float directly over screen content, no box around it), and it was hardcoded to the "normal" palette even though Home switches to the "quiet"/Taking Time palette while a Hold period is open — the two could visibly mismatch. Fixed both: `wrapper` no longer paints any background at all. The pill itself (`pillShape`/`pillContent`) is now a `BlurView` (`expo-blur`, `intensity={40}`, `tint` following `isDark`) with a translucent tint overlay on top (`${colors.surfaceStrong}B3`, alpha-blended rather than a flat fill) clipped to the pill's own rounded corners via `overflow: "hidden"` — a frosted-glass surface, not a solid one. Theme-awareness needed a new mechanism: `BottomTabBar` sits alongside the tab screens, not inside Home's own render tree, so it has no way to read Home's current `homeState` directly. New `QuietPaletteContext` (`src/context/`) is the shared source of truth — Home writes `isQuiet` to it whenever `homeState` changes (`taking-time` → true, everything else → false); `BottomTabBar` reads it and picks its palette (`useAppTheme(isQuiet ? "quiet" : "normal")`) the same way Home itself does. Provider mounted in `app/_layout.tsx`, as high as `HoldFlowProvider`.

**Item 4 — real bug, not a naming choice: `finishReconnecting()` (`app/(tabs)/index.tsx`) sent the Post-Reconnect "Finish Reconnecting" action to `/library`.** That's a dead end — plain Library browsing, with no way back into Reconnect's own remaining steps (the OOO/status gate, the bundled-Circle naming opportunity, Done, the Transition screen). Changed to `/return/reconnect`, matching the sibling `continueReconnecting` case just above it — `reconnect.tsx` re-derives its own state from durable storage on focus, so resuming the actual flow there is correct and sufficient, not just a redirect.

**hold-book updated**: `04-ux-content/04-navigation-architecture.md`'s tab-bar section gets the frosted/theme-aware pill correction in place of the flat-box description; `08-decisions/01-decision-log.md` gets a new summary row for this pass.
