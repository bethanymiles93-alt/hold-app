node_modules

# Cloudflare Workers (worker/) local dev state and secrets — never committed.
.wrangler
worker/.dev.vars

# App-side EXPO_PUBLIC_AI_CLIENT_KEY — same trade-off as the Worker's
# APP_CLIENT_KEY (a shared key, not a strong secret, but still not committed).
.env
.env.local

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
*.log
/.expo/dev/logs
.expo/dev/logs/start.log
*.log
/.expo/dev/logs
/.expo/dev/logs
.expo/dev/logs/start.log
